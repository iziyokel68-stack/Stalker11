# ☢️ СТАЛКЕР ПДА — Мастер-спецификация (v6.0)
*Последнее обновление: 2026-08-17 | Единственный источник истины по протоколу, железу и форматам*

> Протокол, пины, форматы чипов. План — `ROADMAP.md`. Заливка — `proshivki/FLASHING.md`. Каналы — `docs/COMMUNICATION_CANON.md`. Правила для людей — `ИГРОВОЙ_СПРАВОЧНИК.md`. Прогрессия (числа) — `docs/PROGRESSION.txt`. PCB сейчас — `hardware/PCB_PROJECT.md` (V5; v3 в этом файле — архив пинов).

---

## 📁 ЧАСТЬ 1: Архитектура проекта

### Карта проекта

```
Stalker/
├── MASTER_SPECIFICATION.md   ← протокол, пины, чипы
├── ROADMAP.md                ← что сделано / не сделано
├── ИГРОВОЙ_СПРАВОЧНИК.md     ← правила для людей
├── docs/COMMUNICATION_CANON.md
├── docs/PROGRESSION.txt      ← уровни, ранги, достижения
├── proshivki/FLASHING.md     ← как заливать ESP и BU03
│
├── proshivki/
│   ├── Pda/V1/PDA_ESP32/PDA_ESP32.ino
│   ├── Anomaly_GZ/Field_ESP32.ino
│   ├── Terminal/Terminal_ESP32/
│   ├── Cip_Programmer/Chip_Programmer_ESP32.ino
│   └── common/               ← eeprom_*.h, mux_channels.h, tft_panel.h
│
├── stalker_app/              ← программа мастера
├── programmat_pc/            ← protocol.py, game_logic.py, programmer
├── test_firmware/            ← стенды, не полигон
└── hardware/PCB_PROJECT.md   ← PCB V5; v3.0 — архив
```

### Назначение файлов

| Файл | Запускается? | Роль |
|------|:-----------:|------|
| `stalker_app/main.py` | ✅ Да | Программа мастера (событие, регистрация, оповещения, программатор) |
| `programmer.py` | ✅ Да | Программатор устройств (ЧИП / АНОМАЛИЯ / УБЕЖИЩЕ / ПДА / ТЕРМИНАЛ) |
| `game_logic.py` | ❌ Модуль | Вся игровая логика (эталон правил) |
| `protocol.py` | ❌ Модуль | Эталон протокола v4.2, **НЕ МЕНЯТЬ** |

### Запуск

```bash
cd stalker_app
pip install -r requirements.txt
python main.py               # Программа мастера (включает программатор)

# только программатор устройств:
cd programmat_pc
python programmer.py
```

---

## 📡 ЧАСТЬ 2: Протокол связи v4.2

### Структура пакета

```
┌──────────┬──────────┬────────────┬────────────┬────────────┐
│ emitter  │ msg_type │    val1    │    val2    │    val3    │
│  1 байт  │  1 байт  │  2 байта   │  2 байта   │  2 байта   │
│  uint8   │  uint8   │   int16    │   int16    │   int16    │
└──────────┴──────────┴────────────┴────────────┴────────────┘
           Итого: 8 байт | '<BBhhh' | Little-Endian
```

> ⚠️ **Типы урона (`DmgType`) и команды (`CmdSub`) — НЕ поля пакета!**
> Их значения записываются в `val1`/`val2`. Поля `subtype` у `Packet` нет.

### Поле emitter

| Значение | Константа | Кто отправляет |
|:---:|---|---|
| `0` | `Emitter.SYSTEM` | Сервер, глобальные события |
| `1` | `Emitter.PLAYER` | Другой ПДА |
| `2` | `Emitter.ANOMALY` | Устройство-аномалия |
| `3` | `Emitter.BASE` | Убежище / Зелёная Зона |

### Поле msg_type

| ID | Константа | val1 | val2 | val3 |
|:---:|---|---|---|---|
| `0` | `Msg.PING` | ID устройства | Тип устройства | — |
| `1` | `Msg.DAMAGE` | Урон (кол-во) | DmgType **или** 0 | 0 **или** dmg_mask |
| `2` | `Msg.HEAL` | Лечение (HP) | — | — |
| `3` | `Msg.RADIATION` | +Радиация | — | — |
| ~~`4`~~ | ~~резерв~~ | *Деньги теперь через COMMAND + CmdSub.ADD_MONEY* | | |
| `5` | `Msg.COMMAND` | **CmdSub** | Параметр | — |
| `6` | `Msg.ACK` | HP игрока | — | — |
| `7` | `Msg.SAFE_ZONE` | *см. подтипы val3 ниже* | | |
| `8` | `Msg.EMISSION` | Таймер (сек) | Длительность (сек) | — |
| ~~`10`~~ | ~~`Msg.COMBO`~~ | *Удалён. Заменён форматом DAMAGE+dmg_mask* | | |
| `11` | `Msg.RADIO` | Номер трека | Громкость (0=стандарт) | — |

> **DAMAGE — формат пакета (только HP):**
>
> | Формат | val1 | val2 | val3 | Когда использовать |
> |---|---|---|---|---|
> | Старый | урон | DmgType (0-7) | `0` | Один тип урона (обратная совместимость) |
> | **Новый** | урон | `0` | dmg_mask | Несколько HP-типов одновременно |
>
> ⚠️ **РАДИАЦИЯ больше НЕ передаётся через DAMAGE!**
> Для начисления RAD аномалия шлёт отдельный пакет `Msg.RADIATION` с собственным таймером.
> Пакеты HP и RAD полностью независимы — разные интервалы, разные кулдауны.

### Подтипы `Msg.SAFE_ZONE` (val3) — УБЕЖИЩЕ

*Все пакеты `Emitter.BASE + Msg.SAFE_ZONE` обновляют `lastSafeZoneMs` на ПДА → лампочка зелёная.
При отсутствии пакетов > 5 сек: `isInSafeZone()` = false, лампочка гаснет.*

| val3 | Название | val1 | val2 | Действие на ПДА |
|:---:|---|---|---|---|
| `0` | Хил-тик | HP за тик | RAD-очистка за тик | +HP, -RAD |
| `0x00FF` | Beacon | 0 | 0 | Только обновляет таймер зоны, HP не меняет |
| `0x7FFF` | Защита | Тип (0-7) | % (0-100) | `zoneProt[val1] = val2` |

**Архитектура рассылки убежища (`proshivki/Anomaly_GZ/Field_ESP32.ino`):**
```
Каждые sz_hp_frq сек:   хил-тик  (val3=0,    HP=sz_hp, RAD=sz_rad)
Каждые 2 сек:           beacon   (val3=0xFF, если защиты нет)
                        защита   (val3=0x7FFF, если zoneProt[i] > 0)
  ⇒ цикл по 8 типам защиты, все обновлены за 16 сек
Предел: innate[i] + zone[i] ограничен до 100%
```

**Выброс и защита убежища:**
- В программаторе — галочка `sz_emission` (защита от выброса)
- В `Field_ESP32.ino`: `cfgSzEmit` уходит в beacon `SAFE_ZONE` (val1, val3=`SZ_BEACON_FLAG`)
- В `PDA_ESP32.ino`: `MSG_EMISSION` / `CONFIG:EMISSION` запускают отсчёт; удар 250/250 каждые 10 с, если не `isInSafeZone() && szEmissionProtect`

### DmgType — HP-типы урона (биты dmg_mask)

**7 активных HP-типов (биты 0-6 маски dmg_mask):**
*(Отображение в UI зависит от проекта — задаётся `CmdSub.SET_PROJECT`)*

| Бит dmg_mask | Константа | S.T.A.L.K.E.R. | Fallout | Metro | Tarkov | Действие |
|:---:|---|---|---|---|---|---|
| `0` | `DmgType.EXPLOSION` | Взрыв | Мины | Граната | Граната | -HP |
| `1` | `DmgType.BLEED` | Кровь | Ранение | Укус | Кровотечение | -HP |
| `2` | `DmgType.THERMAL` | Термо | Огонь | Огонь | Ожог | -HP |
| `3` | `DmgType.ELECTRIC` | Электро | Энерго | Шокер | Шок | -HP |
| `4` | `DmgType.CHEMICAL` | Химия | Токсин | Гр.воздух | Отравление | -HP |
| `5` | `DmgType.PSY` | Пси | Ментал | Ужас | Контузия | -HP |
| `6` | `DmgType.GRAVITY` | Гравитация | Плазма | Аномалия | Перелом | -HP |
| ~~`7`~~ | — | **Свободен** — RAD идёт через отдельный `Msg.RADIATION` | | | | — |

> ⚠️ **Бит 7 свободен.** RADIATION — это отдельный пакет `Msg.RADIATION`, не бит в dmg_mask.

> ⚠️ **Бит 5 зарезервирован.** ПДА его игнорирует при обработке dmg_mask.
> Радиация начисляется **только** пакетом `Msg.RADIATION` (val1 = кол-во RAD),
> с отдельным таймером и отдельным кулдауном.

**Коды команд (val1 при COMMAND):**

| Значение | Константа | Действие |
|:---:|---|---|
| `0` | `CmdSub.KILL` | Принудительная смерть |
| `1` | `CmdSub.REVIVE` | Воскрешение (heal при `is_dead`; не снимает `admit_pending`) |
| `2` | `CmdSub.WIPE_DATA` | Сброс до заводских |
| `3` | `CmdSub.ADD_MONEY` | Начислить деньги (val2=сумма) |
| `4` | `CmdSub.SET_PROJECT` | Сменить проект (val2=project_id) |
| `5` | `CmdSub.ADD_XP` | Начислить опыт (val2=кол-во) |
| `6` | `CmdSub.GRANT_RESISTANCE` | Увеличить сопротивление (val2=DmgType) |
| `7` | `CmdSub.ADMIT` | Допуск в игру (снять `admit_pending`; только главный мастер) |

### Примеры (Python)

```python
from protocol import Packet, Msg, DmgType, CmdSub, Emitter
from protocol import create_damage_packet, create_damage_multi_packet
from protocol import create_radiation_packet, create_command_packet

# HP-урон от аномалии (один тип — старый формат)
pkt = create_damage_packet(amount=15, dmg_type=DmgType.THERMAL)

# HP-урон от аномалии (несколько типов — новый формат)
# dmg_mask = 0b00010100 = 20 (ТЕРМО + ХИМИЯ)
pkt = create_damage_multi_packet(amount=15, dmg_mask=0b00010100)

# RAD — ОТДЕЛЬНЫЙ пакет, отдельный таймер!
pkt = create_radiation_packet(rad_amount=5)   # +5 RAD, не зависит от HP-удара

# Зелёная зона (+2HP/тик, -1RAD/тик, 0 Пси)
pkt = Packet(emitter=Emitter.BASE, msg_type=Msg.SAFE_ZONE,
             val1=2, val2=1, val3=0)

# Выброс (через 300 сек, длится 60 сек)
pkt = Packet(emitter=Emitter.SYSTEM, msg_type=Msg.EMISSION,
             val1=300, val2=60)

# Команда убить
pkt = create_command_packet(cmd=CmdSub.KILL)

# Сериализация
data: bytes = pkt.pack()         # → 8 байт
pkt2  = Packet.unpack(data)      # ← 8 байт
```

### Сетевой транспорт (UDP — эталон `game_logic.py`, dev-тесты)

| Устройство | IP | Порт |
|---|---|:---:|
| ПДА (слушает входящие) | `127.0.0.1` | `5000` |
| Аномалия (слушает ACK) | `127.0.0.1` | `5001` |

> На реальном железе: ESP-NOW вместо UDP. Пакет тот же 8-байтный.

---

## ⚙️ ЧАСТЬ 3: Железо (ESP32-S3-N16R8)

### Принятые решения по железу (июнь 2026)

| Тема | Решение |
|---|---|
| **Эталон схемы** | `hardware/schematic.html` + `docs/komponenty.txt` |
| **Эталон PCB** | `hardware/STALKER_PDA_v3.0/` — две платы LOGIC + POWER |
| **Пассивка ПДА** | Полный список в BOM ниже; **R8 10 kΩ** на затвор P-MOS BU03 — обязателен |
| **Физические ключи** | **PWR** (разрыв АКБ). **SND нет** — громкость в НАСТРОЙКАХ на экране. **VIB** (разрыв 5V→вибро, если установлен). **BL** — входы G37/G38; LED TFT → 3V3. **G47/G48 не используются** |
| **EEPROM** | **5 слотов TCA** (CH0, CH2, CH3, CH5, CH6): TRRS-картриджи + **J_TRS** шлейф на **CH0** (касса/терминалы); эталон каналов — `proshivki/common/mux_channels.h` |
| **DC-DC ПДА** | **2× TPS63020**: → **5V** (DFPlayer, вибро LRA), → **3.3V** (ESP, TFT, LoRa, BU03, TCA) |
| **Вибромотор** | Питание **только 5V BUS** через **физический ключ VIB**; импульсы G21 → 220 Ω → 2N2222 (только при поданном питании на ветку); flyback **1N5819** |
| **BU03 на плате** | UART **Serial1 G1/G2**; питание **G42** (2N2222 + P-MOS); `BU03_PINS_PLATE 1` в тест-прошивке |
| **Дисплей** | **M024-240320** SPI 2.4" (драйвер Adafruit ILI9341). Стекло **320×240**. После `begin()` — `tftApplyPanel()` из `tft_panel.h` (MADCTL **0x88**, без MV). Не `setRotation(0/2)` |
| **Полевые устройства** | ESP32-S3 + **BU03 + LoRa**; пины LoRa/BU03 **как на ПДА** |
| **Потребление** | `hardware/POTREBLENIE.pdf` |
| **Статус проекта (июнь 2026)** | Схема и железо **утверждены**; KiCad v3.0 **разведён и проверен PCB-специалистом**; **сборка платы** — следующий шаг. Стенды BU03/LoRa/`Test_PDA_Hardware` ✅; боевая UWB-логика аномалий — в работе (фаза 4) |
| **LoRa** | **Только Мастер-Пульт** и полигон (выброс, админ-команды, радио, ретрансляция). **Не** для кассы/банка/квестов |
| **UWB (BU03)** | **Точная** дистанция и вход в зону; калибровка `AT+SETDEV` на полигоне |
| **RSSI** | **Не** для позиционирования — только грубое «сигнал / устройство рядом» (детектор, якоря LoRa) |
| **Терминалы** (банк, касса, квесты, мастерская) | **EEPROM на CH0** — handoff-чип @ `0x00` или кабель + TXN @ `0x80`; эталон `eeprom_txn.h`, `mux_channels.h` |

> **Сводка каналов:** см. таблицу в `ROADMAP.md` (Фаза 4) и `docs/COMMUNICATION_CANON.md`.

### PCB ПДА v3.0 (две платы-сэндвич)

| Плата | Размер | Контур | Файл KiCad |
|-------|--------|--------|------------|
| **LOGIC** | **140 × 78 mm** | полигон с вырезами (TFT, BU03) | `STALKER_PDA_LOGIC.kicad_pcb` |
| **POWER** | **140 × 78 mm** | прямоугольник | `STALKER_PDA_POWER.kicad_pcb` |

Связь плат: **J_INTER 1×15** (шаг 2.54 mm). Краткая спека для разработчика PCB: `hardware/BOARD_SPEC_v3_FOR_REVIEW.md`.

> Расстановка компонентов и контур Edge.Cuts — **только KiCad v3.0** (gerber от 17.06.2026).

### Таблица пинов ПДА (ESP32-S3-N16R8)

> ⚠️ **Аудит GPIO (июнь 2026):** на PCB v3 **занято 26 пинов** — **свободных GPIO для новой периферии нет.** Полная карта 0–48 — в приложении «Карта GPIO» ниже.

> ⚠️ **Запрещённые пины:** GPIO 0, 3, 19, 20, 35, 36, 45, 46 (strapping / USB / flash) · GPIO 43, 44 — **U0TXD/U0RXD** (USB Serial-JTAG на DevKit; **на PCB не разведены**, **не использовать** под TFT_BL)

| Периферия | GPIO | Назначение |
|---|:---:|---|
| **TFT ILI9341 (SPI)** | 10 | CS |
| | 11 | DC |
| | 12 | RST (shared TFT + LoRa) |
| | 13 | SCK |
| | 14 | MOSI |
| | — | **LED (подсветка):** пин LED на J_TFT → **3V3** (постоянно при питании); **`TFT_BL` не назначен** — см. § подсветка |
| **Кнопки (INPUT_PULLUP)** | 4 | OK |
| | 5 | ESC |
| | 6 | RIGHT |
| | 7 | DOWN |
| **LED** | 15 | Красный (220Ω) |
| | 16 | Зелёный (100Ω) |
| **I2C → TCA9548A** | 8 | SCL |
| | 9 | SDA |
| **DFPlayer (UART)** | 17 | TX → DFP RX (через 1кΩ) |
| | 18 | RX ← DFP TX |
| **Вибромотор (NPN)** | 21 | База 2N2222, питание мотора 5V (**после** физического ключа VIB) |
| **Тумблер BL (INPUT_PULLUP)** | 37 | Положение **ON** (замыкание на GND) |
| | 38 | Положение **AUTO** (замыкание на GND) |
| **G47 / G48** | 47–48 | **Не используются** (ключа SND нет; громкость в НАСТРОЙКАХ) |
| **LoRa SX1278 (SPI)** | 39 | CS |
| | 40 | MISO (shared) |
| | 41 | DIO0 |
| **UWB BU03-Kit (UART)** | 1 | RX ← BU03 TX (PA2) |
| | 2 | TX → BU03 RX |
| | 42 | BU03_PWR (2N2222 + P-MOS → 3.3 V) |

> Схема: `hardware/schematic.html` · PCB: `hardware/STALKER_PDA_v3.0/`  
> SPI shared: SCK=G13, MOSI=G14, MISO=G40. LoRa RST = TFT RST = **G12**.  
> UART: BU03 → **Serial1** G1/G2 (`BU03_PINS_PLATE 1`) · DFPlayer → **Serial2** G17/G18.

#### Подсветка TFT — железо vs прошивка (аудит 26.06.2026)

| Слой | Поведение на **PCB v3** |
|---|---|
| **Железо** | Пин **LED** разъёма J_TFT подключён к **3V3** — подсветка **всегда включена** при поданном питании. Трёхпозиционный тумблер **BL** на корпусе **не разрывает** цепь LED; контакты **ON→G37**, **AUTO→G38**, общий **GND** |
| **Прошивка** | Читает положение BL (G37/G38) для **логики UI** (режим ON / AUTO / OFF). **Не может** выключить или затемнить подсветку без изменения железа |
| **AUTO (10 с idle)** | Затемнение по таймеру простоя — **требует GPIO на линии LED** (PWM/MOS); на текущей плате **невозможно** |
| **История** | В ранних тест-скетчах `TFT_BL` был на **G21** — на v3 **G21 = вибро** |

**Варианты на будущее (не v3):**

1. Оставить LED на 3V3; позиции BL — только UI (текущее состояние).
2. Механика: в OFF тумблер **физически** обрывает питание LED; AUTO = всегда включена физически.
3. **PCB rev v3.1+:** LED через N-MOS от GPIO (нужен свободный пин — сейчас **нет** без компромисса).
4. **Не использовать G43/G44** под TFT_BL — потеря USB-прошивки и Serial-логов.

> **Итог v3:** подсветка **не управляется** GPIO (LED → 3V3). Тумблер BL — только **ON/OFF** для логики UI (G37/G38). Программное AUTO/затемнение **не планируется** на этой ревизии платы.

#### Панель TFT M024-240320 (собрано, 17.08.2026)

Модуль на плате — **M024-240320** SPI 2.4". Стекло **альбом 320×240**. Контроллер ведёт себя как **ILI9342**: бит **MV** в MADCTL **игнорируется**. Adafruit ILI9341 `setRotation()` одна **не** заполняет матрицу.

| Действие | Результат |
|---|---|
| `setRotation(0)` или `(2)` — GFX 240×320 | полоса **справа** ~80 px |
| `setRotation(1)` или `(3)` без правки MADCTL | полоса **снизу** ~80 px (картинка «замирает») |
| `setRotation(1)` + MADCTL `0xC8` (MX\|MY\|BGR) | весь экран, **отзеркалено** |
| **`setRotation(1)` + MADCTL `0x88` (MY\|BGR)** | **весь экран, ориентация OK** |

**Эталон кода:** `proshivki/common/tft_panel.h` → `tftApplyPanel(tft)` сразу после `tft.begin()`.  
**Проверка:** `test_firmware/TFT_Standalone_Test/`.  
**BGR:** красный/синий в RGB565 меняются местами (`TFT_BGR_SWAP`).

Не ставить «книжный» UI (rotation 0/2) на эту панель — 80 px стекла останутся пустыми.

### Сборка ПДА v3.0 — что паять

*Чеклист сборки в `ROADMAP.md` (фаза 3); полный BOM — `docs/komponenty.txt`.*

| Компонент / действие | Плата / примечание |
|---|---|
| PCB LOGIC + POWER | `hardware/STALKER_PDA_v3.0/` |
| J_INTER 1×15 | соединение плат-сэндвич |
| Female socket 2×22 | ESP32-S3 DevKit |
| TFT ILI9341 2.4" | разъём **J_TFT** → корпус; LED J_TFT → **3V3** |
| LoRa Ra-01 | POWER |
| **Антенна LoRa 433 МГц** | **пружина/шток на Ra-01** — обязательна для TX |
| BU03-Kit | POWER, UART через J_INTER (G1/G2) |
| DFPlayer Mini | POWER |
| TCA9548A | LOGIC |
| TRRS PJ-392 ×4 + шлейф **J_TRS** | CH0 кабель терминалов + CH2/CH3/CH5/CH6 картриджи |
| 4× кнопки, LED×2 | LOGIC / корпус |
| 2× 2N2222 + P-MOS AO3401 | вибро G21 + BU03_PWR G42 |
| 4× кнопок + ключ PWR (+ VIB/BL если есть) | **PWR**; **SND нет** (громкость на экране); **BL** (G37/G38) |
| 2× TPS63020 | 5V и 3.3V |
| Пассивка | **§ «Пассивка и обвязка LOGIC/POWER»** ниже · эталон `schematic.html` |
| На корпусе (провода) | динамик **J_SPK**, вибро **J_VIB**, **JST BAT**, MicroSD в DFPlayer |

#### Пассивка и обвязка LOGIC/POWER v3.0

*Номиналы и refdes — `hardware/schematic.html` (эталон логики). Расстановка — `hardware/STALKER_PDA_v3.0/*.kicad_pcb`. Сводный BOM — `docs/komponenty.txt`.*

> KiCad-схемы v3.0 — **stub** (без netlist); на шелкографии PCB refdes могут отличаться от эталона (см. колонку «Silk PCB»). **Не путать:** footprint `PDA_ST:R 220` используется для разных номиналов — ориентир по **номиналу из таблицы**, не по Value в KiCad.

**Итого на ПДА:** 9 резисторов · 4 конденсатора · 2× 2N2222 · 1× P-MOS · 1× диод · 2× гребёнка 2×22 (ESP32).

##### Резисторы

| Ref | Номинал | Корпус | Назначение | Плата | Silk PCB / где искать |
|:---:|---------|--------|------------|:-----:|-----------------------|
| **R1** | 4,7 кОм | THT ¼ W | Подтяжка I2C **SDA** (G9) → 3V3; одна пара на шине ESP↔TCA | **LOGIC** | `R4.7K` ×2, у TCA/ESP (~88 mm, шина I2C) |
| **R2** | 4,7 кОм | THT ¼ W | Подтяжка I2C **SCL** (G8) → 3V3 | **LOGIC** | см. R1 |
| — | 220 Ω | THT ¼ W | **G15** → LED красный (Damage/Dead) | **LOGIC** | `R220` у штыря **G-LED-R** (~113 mm) |
| — | 100 Ω | THT ¼ W | **G16** → LED зелёный (Safe Zone) | **LOGIC** | `R220` / cluster рядом с G-LED-R (~113 mm); **не 220 Ω** |
| **R6** | 1 кОм | THT ¼ W | **G17** (UART TX) → RX DFPlayer (ограничение тока) | **POWER** | `R 1K`, у DFPlayer / линии UART |
| **R7** | 10 кОм | THT ¼ W | Подтяжка **Shared RST G12** (TFT RST + LoRa RST) → 3V3 | **LOGIC** | ⚠️ отдельный footprint **не найден** в KiCad PCB — см. «Пробелы» |
| **R8** | 10 кОм | THT ¼ W | Подтяжка **затвора P-MOS** BU03 → 3V3 (OFF при HIGH gate) | **POWER** | `R10K`, у **AO3401** / BU03_PWR |
| — | 220 Ω | THT ¼ W | **G21** → база **Q1** (ключ вибромотора) | **POWER** | `R220`, у **Q1** / **J_VIB** |
| — | 220 Ω | THT ¼ W | **G42** → база **Q2** (BU03_PWR) | **POWER** | `R220`, у **Q2** / BU03-Kit |

##### Конденсаторы

| Ref | Номинал | Корпус | Назначение | Плата | Silk PCB / где искать |
|:---:|---------|--------|------------|:-----:|-----------------------|
| **C1** | 100 нФ | керам. THT диск 5 mm | Развязка **LoRa Ra-01** VCC/GND | **POWER** | `100n`, у **U5** Ra-01 (~95 mm) |
| **C2** | 100 нФ | керам. THT диск 5 mm | Bypass **BU03 VCC/GND у модуля** (на шине 3V3 **до** P-MOS, не после ключа) | **POWER** | ⚠️ в KiCad только **2×** `100n` — второй у LoRa; C2 у BU03 см. «Пробелы» |
| **C3** | 100 нФ | керам. THT диск 5 mm | Развязка **DFPlayer** 5V/GND | **POWER** | `100n`, у **U4** DFPlayer (~157 mm) |
| **C4** | 10 нФ | керам. THT диск 5 mm | Debounce **Shared RST G12** → GND | **LOGIC** | `C 10n`, у пина **G12** DevKit (~179 mm) |

##### Транзисторы и ключи

| Ref | Тип | Корпус | Назначение | Плата | Silk PCB / ориентация |
|:---:|-----|--------|------------|:-----:|------------------------|
| **Q1** | **2N2222** NPN | TO-92 | Ключ **вибромотора LRA**: коллектор → M−, эмиттер → GND; база ← 220 Ω ← G21; питание M+ = 5V **после** ключа VIB | **POWER** | `Q1` «2N2222 VIB», у **J_VIB** |
| **Q2** | **2N2222** NPN | TO-92 | Драйвер **BU03_PWR**: коллектор → gate P-MOS, эмиттер → GND; база ← 220 Ω ← G42 | **POWER** | второй `Q1` на silk (должен быть **Q2**), у BU03 |
| — | **AO3401** P-MOS | SOT-23 | High-side ключ 3V3 → **VCC BU03**: **S**→3V3, **D**→VCC модуля, **G**←Q2; **R8** gate→3V3 | **POWER** | `Q` «PMOS»; пины **1=G 2=S 3=D** |

##### Диоды

| Ref | Тип | Корпус | Назначение | Плата | Silk PCB |
|:---:|-----|--------|------------|:-----:|----------|
| **D1** | **1N5819** Шоттки | THT DO-41 | Flyback вибромотора: **анод** → линия M− (коллектор Q1), **катод** → M+ / 5V | **POWER** | `DIOD`, у **J_VIB** / LRA |

##### Посадочные места и разъёмы (плата)

| Ref / разъём | Назначение | Плата |
|---|---|:---:|
| **Female 2×22** ×2 | Сокет под **ESP32-S3 DevKitC-1** | LOGIC |
| **J_INTER 1×15** | Межплатная шина LOGIC↔POWER (питание, GND, UART BU03, сигналы) | обе |
| **J_TFT** | Шлейф ILI9341; пин **LED** → **3V3** (не GPIO) | LOGIC |
| **J_BTN** | 4× кнопки с корпуса | LOGIC |
| **J3/J4 × TRRS** + **J8 J_TRS** | Картриджи EEPROM + кабель кассы CH0 | LOGIC |
| **J_SPK** | Динамик 8 Ω | POWER |
| **J_VIB** | Вибромотор LRA + цепь Q1/D1 | POWER |
| **JST BAT** | Аккумулятор Li-Po (через PWR на корпусе) | POWER |

##### Пробелы и расхождения KiCad vs `schematic.html`

| Элемент | Статус |
|---|---|
| **R7 10 kΩ** (RST G12) | В `schematic.html` и BOM обязателен; **отдельного footprint на LOGIC PCB не видно** — проверить при сборке по трассам у G12 или допаять вручную |
| **C2 100 nF** (BU03) | В эталоне три 100 nF; на POWER PCB разведено **два** (`100n`) — у LoRa и DFPlayer; bypass у BU03 по схеме **обязателен** |
| Refdes Q1/Q2 | На POWER оба NPN подписаны `Q1`; второй — **Q2 BU03_PWR** |
| Value в KiCad | У `R4.7K`, `R10K`, `R 1K` поле Value часто `R 220` — **игнорировать**, брать номинал из этой таблицы |
| KiCad `.kicad_sch` | Stub без компонентов; BOM только из `schematic.html` + PCB |

**После пайки:** `test_firmware/Test_PDA_Hardware/` (`BU03_PINS_PLATE 1`) → проверка 3V3/5V/GND до первого включения ESP.

#### Приложение: карта GPIO 0–48 (ESP32-S3-N16R8, PDA v3)

| GPIO | Статус | Назначение / примечание |
|:---:|:---:|---|
| 0 | ⛔ | Strapping — **запрещён** |
| 1 | ✅ | BU03 UART RX |
| 2 | ✅ | BU03 UART TX |
| 3 | ⛔ | Strapping / JTAG — **запрещён** |
| 4 | ✅ | Кнопка OK |
| 5 | ✅ | Кнопка ESC |
| 6 | ✅ | Кнопка RIGHT |
| 7 | ✅ | Кнопка DOWN |
| 8 | ✅ | I2C SCL → TCA9548A |
| 9 | ✅ | I2C SDA → TCA9548A |
| 10–14 | ✅ | TFT SPI (CS, DC, RST, SCK, MOSI) |
| 15–16 | ✅ | LED красный / зелёный |
| 17–18 | ✅ | DFPlayer UART TX / RX |
| 19–20 | ⛔ | USB D− / D+ — **запрещён** |
| 21 | ✅ | Вибромотор (база 2N2222) |
| 22–25 | — | **Не существуют** на чипе |
| 26–34 | 🔒 | Octal Flash / PSRAM (N16R8) — **внутренние**, на DevKit не выведены |
| 35–36 | ⛔ | Flash / PSRAM strapping — **запрещён** |
| 37–38 | ✅ | Входы тумблера BL (ON / AUTO), INPUT_PULLUP |
| 39–41 | ✅ | LoRa CS / MISO / DIO0 |
| 42 | ✅ | BU03_PWR |
| 43–44 | 🔌 | **U0TXD / U0RXD** (USB Serial-JTAG) — на штырьках DevKit, **на PCB v3 не разведены** |
| 45–46 | ⛔ | Strapping — **запрещён** |
| 47–48 | — | **Не используются** (SND нет; громкость в НАСТРОЙКАХ) |
| — | — | **Свободных GPIO на PCB v3 нет** |

### Пины аномалии / убежища (ESP32-S3-N16R8)

> На полевых устройствах используются **те же GPIO**, что в таблице выше, только нужные строки:

| Периферия | GPIO | Назначение |
|---|---:|---|
| **LoRa SX1278 (SPI)** | 39 | CS |
| | 40 | MISO (shared) |
| | 41 | DIO0 |
| | 12 | RST (+ 10 kΩ → 3V3, 10 nF → GND) |
| | 13 | SCK (shared) |
| | 14 | MOSI (shared) |
| **UWB BU03-Kit (UART)** | 1 | RX ← BU03 TX |
| | 2 | TX → BU03 RX |
| | 42 | BU03_PWR (220 Ω → 2N2222 → P-MOS → 3.3 V) |
| **LED (только ЗЗ, cat=1)** | 16 | Зелёный (100 Ω → 3V3) |

TFT, I2C, DFPlayer, кнопки, вибро, TCA — **не подключаются**.

### UWB — BU03-Kit (принято, июнь 2026)

| Параметр | Решение |
|----------|---------|
| Модуль | **BU03-Kit** (STM32 + DW3000) на **ПДА**, **аномалии** и **убежище** |
| Алгоритм | **TWR** только (`AT+SETUWBMODE=0`) |
| Канал | **Один на всю экосистему:** `CH:1` (ch5), `Rate:1` |
| Дистанция | Считает **STM на BU03**; ESP — **UART AT** |
| Питание | Шина **3.3 V**; отключение **ПДА, аномалии и убежища** — GPIO + **2N2222 + P-MOS** на VCC |
| Схема HTML | `hardware/schematic.html` — BU03 UART G1/G2, G42 |
| Схема PCB | `hardware/STALKER_PDA_v3.0/` — BU03 на POWER, UART через J_INTER |

**Макет (breadboard):** BU03 на G17/G18 только для временной отладки (`BU03_PINS_PLATE 0`). **На плате — только G1/G2.**

**AT (115200, `\r\n`):** `AT`, `AT+GETCFG`, `AT+SETCFG`, `AT+SAVE`, `AT+RESTART`, `AT+DISTANCE` (на anchor), `AT+SETDEV` (калибровка a/b).

**Калибровка:** `AT+SETDEV` на каждой точке на полигоне; `CAL_OFFSET_M` в тест-скетче — только временно.

**Тесты (по отдельности):** `BU03_Distance_AT.ino`, `BU03_ID_Test.ino`.  
**Интеграция (всё на одном S3):** `Test_PDA_Hardware/` — ✅ PASS (июнь 2026).  
**ПДА (тест дистанции):** `PDA_ESP32.ino` v2.7+ — страница UWB, `AT+DISTANCE` ~750 ms.

#### Статус UWB (июнь 2026)

| Область | Статус |
|---|---|
| Стенд BU03 (`BU03_Distance_AT`, `BU03_ID_Test`, `Test_PDA_Hardware`) | ✅ |
| ПДА: UART G1/G2, `BU03_PWR` G42, опрос дистанции | ✅ v2.7 |
| ЗЗ: стенд anchor↔tag, порог входа | ✅ настроено на стенде |
| ЗЗ: ESP-NOW beacon (`Field_ESP32` cat=1) | ✅ фаза 2.5 |
| Аномалия: слоты, `ZONE_ASSIGN`, `AT+DISTANCE` в `Field_ESP32` | ⏳ не в коде |
| ПДА: `ENTRY_OK`, `uwb_zone_id` в боевой логике | ⏳ |
| Включение BU03 по RSSI при приближении | ⏸ после сборки платы |
| Калибровка `AT+SETDEV` на полигоне | ⏳ |

#### Совмещение TFT + LoRa + BU03 (10.06.2026)

> На макете **по отдельности** работают экран, LoRa, BU03. GPIO **не конфликтуют** при пинах из таблицы выше. Риски — **шины, reset, UART, питание, loop**.

| Риск | Суть | Что делать |
|------|------|------------|
| **SPI shared** | TFT (CS G10) + LoRa (CS G39) на SCK/MOSI/MISO | Один CS = LOW; не вызывать LoRa SPI во время `tft.fillScreen()` |
| **G12 shared RST** | Сброс TFT = сброс LoRa | Не дёргать RST после boot; при reset — re-init **обоих** |
| **Два UART** | BU03 ≠ DFPlayer | BU03: **Serial1** G1/G2 · DFPlayer: **Serial2** G17/G18 |
| **BU03 макет** | На breadboard BU03 на G17/G18 | В `Test_PDA_Hardware`: переназначить `BU03_RX`/`BU03_TX` на G17/G18 |
| **BU03_PWR G42** | Без ключа — нет питания | `HAS_BU03_PWR=1` на плате; `0` если 3.3V напрямую |
| **Питание 3.3V** | LoRa TX + UWB ranging + TFT | **Не одновременно** в прошивке; brownout = ложные «сбои» |
| **Loop blocking** | Долгий redraw → пропуск LoRa/BU03 | `LoRa.parsePacket()` и UART BU03 — часто в `loop()` |
| **RF** | 433 МГц / 2.4 / 6.5 ГГц | Частоты разные; **антенны** не наслаивать |

**Интеграционная проверка:** `Test_PDA_Hardware` — guided on-screen (фазы UWB и LoRa в общем потоке).

#### Принцип работы ESP ↔ BU03

| Слой | Кто | Задача |
|------|-----|--------|
| **UWB / TWR** | STM на BU03 | Дистанция, фильтр, калибровка `SETDEV` |
| **UART AT** | ESP32 | `SETCFG`, `DISTANCE`, `SAVE`, `BU03_PWR` |
| **Игра** | ESP32 | ESP-NOW: урон, ЗЗ, слоты, `ENTRY_OK` |

ESP **не** прошивает STM в поле — только AT-команды. Полное отключение UWB — **срез 3.3 V** (GPIO **G42** → 2N2222 → P-MOS), не «тишина» на UART.

#### Совместимость и ограничения

| Тема | Решение |
|------|---------|
| **BU03 vs DWM1000** | Экосистема — **только BU03-Kit** (UART) |
| **Роли BU03** | Только **0 = tag**, **1 = anchor**; алгоритм **TWR** (`AT+SETUWBMODE=0`) |
| **Канал** | **Один** на весь полигон: `CH:1`, `Rate:1` — все устройства в одной экосистеме |
| **ID 0–10** | Два tag с **одинаковым ID** в зоне одной аномалии — **ошибка**; у аномалии — **динамические слоты** |
| **MAC ≠ UWB ID** | ESP-NOW MAC и UWB ID разные; связь **slot ↔ MAC ↔ имя** в NVS |
| **AT-интерфейс** | Команды только на **UART1 (TX1/RX1)**; PA2/PA3 — бинарный поток, в финале не используем |
| **Макет vs плата** | Макет: TX1/RX1 ↔ **G17/G18** (проверено); плата: **G1/G2** (DFPlayer на G17/G18) |

#### Калибровка

- Завод: `a=1.0`, `b=0.0`, antenna delay `16336`.
- Полигон: Excel Ai-Thinker → `AT+SETDEV` (**a**, **b**) на **каждой** точке (аномалия / ЗЗ).
- `CAL_OFFSET_M` в тест-скетче — **временный костыль**, не финал.

### LoRa SX1278 Ra-01 — радио и протокол (принято, 9 июня 2026)

> LoRa — **дальняя связь** (команды, события, ретрансляция). **UWB (BU03)** — метры рядом с точкой. **ESP-NOW** — короткая дистанция, игровые пакеты (DAMAGE, ACK). Это **разные каналы**, не замена друг другу.

#### Как работает LoRa (не MAC, не Wi-Fi)

| Уровень | Поведение |
|---|---|
| **Физический (SX1278)** | Модуль передаёт **на частоту** 433 МГц. MAC-адресов **нет**. |
| **«Канал»** | Одинаковые **freq + SF + BW + sync word** на всех узлах → все слышат эфир. |
| **Прикладной (наш протокол)** | Адресация **в payload**: `dest_id`, `src_id`, `type`, данные, **CRC16**. |
| **Broadcast** | `dest_id = 0xFF` — пакет для всех; фильтрация в прошивке. |

Передача **не привязана к MAC ESP** — только к настройкам радио и байтам в пакете.

#### Роли каналов в экосистеме (согласовано, 17.06.2026)

| Задача | Технология |
|---|---|
| **Мастер-Пульт:** Выброс, KILL/REVIVE/WIPE, радио-треки, ретрансляция | **LoRa** |
| Точное расстояние до аномалии / вход в ЗЗ | **UWB (BU03)** — калибровка `AT+SETDEV` |
| Грубое «что-то рядом» / слышимость (не метры) | **RSSI** (LoRa или ESP-NOW) — **без** позиционирования |
| Грубая зона на полигоне **до ~3×3 км** (опц.) | **LoRa-якоря:** «какие якоря слышали» (да/нет), **не** RSSI-триангуляция |
| Игровой урон, ACK, beacon зоны | **ESP-NOW** |
| **Внешние терминалы** (банкомат, касса, квесты, мастерская) | **EEPROM на CH0** (чип handoff или кабель TXN @ `0x80`) — **не LoRa** |

**Ретрансляция (будущее):** узел получил пакет → если `dest_id` не свой и `TTL > 0` → переслать в эфир (цепочка ESP на большом полигоне).

#### Пины LoRa — макет vs плата

| Платформа | Когда | SCK | MISO | MOSI | CS | RST | DIO0 |
|---|---|:---:|:---:|:---:|:---:|:---:|:---:|
| **ESP32 DevKit (breadboard)** | Временный тест **до** сборки S3 | 18 | 19 | 23 | 5 | 14 | 26 |
| **ESP32-S3-N16R8 (ПДА / поле)** | Целевая плата | 13 | 40 | 14 | 39 | 12 | 41 |

> На S3: SPI shared с TFT; RST LoRa = RST TFT (**G12**).  
> Тестовые скетчи на breadboard **не используют** пины S3 — при переносе на плату меняются только `#define` пинов.

#### Тестовые прошивки (9 июня 2026)

| Скетч | Папка | Назначение |
|---|---|---|
| Передатчик | `test_firmware/LoRa_Test_Sender/` | Ping каждые 2 с, broadcast `0xFF` |
| Приёмник | `test_firmware/LoRa_Test_Receiver/` | Приём + heartbeat; SPI-диагностика при `init FAILED` |

**Библиотека:** [LoRa by Sandeep Mistry](https://github.com/sandeepmistry/arduino-LoRa) — ставить **вручную** (ZIP с GitHub): Library Manager часто отдаёт **403 Forbidden**.

**Параметры теста (оба узла):** 433 МГц, sync word `0x12`, SF=7, BW=125 kHz, CR=4/5.

**Формат пакета (черновик):**
```
[dest_id][src_id][type][payload...]
  0xFF      1     0x01   "ping #N"
```

**Результат стенда 9.06.2026:**
- Связка ESP32 + Ra-01 на breadboard **работает** (код и распайка верны).
- **Swap-тест:** один из двух модулей Ra-01 **неисправен** (`LoRa init FAILED`, SPI не отвечает) — **заменить**.
- Второй модуль: `LoRa OK`, приёмник слушает эфир; полный ping-pong — после замены мёртвого Ra-01.

**Диагностика неисправного модуля:** переставить Ra-01 на другой ESP — если `init FAILED` переезжает с модулем, виноват **модуль**, не плата.

### BOM (смета)

**ПДА (полный вариант):**

| Компонент | Модель | Цена |
|---|---|:---:|
| Микроконтроллер | ESP32-S3-N16R8 (DevKitC-1) | ~400-600 ₽ |
| Дисплей | TFT 2.4" M024-240320 SPI 320×240 (`tft_panel.h`) | ~250–400 ₽ |
| Аудио-модуль | DFPlayer Mini (MP3) + MicroSD | ~150 ₽ |
| I2C мультиплексор | TCA9548A | ~150 ₽ |
| Разъёмы картриджей | TRRS PJ-392 ×4 + **J_TRS** шлейф на **CH0** | ~50 ₽/шт |
| EEPROM картриджи | 24LC256 / AT24C32 DIP-8 в TRRS-модуле (**внешние**, не на основной PCB) | ~30–80 ₽/шт |
| Вибромотор | LRA + 2N2222 + 1N5819; питание **5V BUS** | ~120 ₽ |
| Аккумулятор | Li-Po 3.7V 1000mAh (от вейпа/одноразки) | Бесплатно (донор) |
| Зарядка | *(вне текущего scope документации)* | — |
| Понижающий DC-DC | TPS63020 → **5V** (5V BUS: DFPlayer, вибро) | ~150 ₽ |
| Повышающий DC-DC | TPS63020 → **3.3V** (3V3 BUS: ESP, LoRa, UWB, TCA) | ~150 ₽ |
| Кнопки + 4× физических ключа (PWR/SND/VIB/BL) | — | ~280 ₽ |

> **Питание ПДА:** Li-Po 3.7V → механический PWR → **две параллельные ветки TPS63020**:
> понижающий модуль на **5V BUS**, повышающий модуль на **3.3V BUS**.
> **5V BUS:** DFPlayer Mini (через **физический ключ SND**), **вибромотор LRA (+)** (через **физический ключ VIB**), ESP 5Vin (опционально).
> **3.3V BUS:** ESP 3V3, TFT VCC, **LED подсветки TFT** (пин LED J_TFT → **3V3**, без GPIO), LoRa, BU03 (через P-MOS), TCA9548A, TRRS Ring2.
> На схеме (`hardware/schematic.html`) подписи «понижающий/повышающий» = подписи ваших модулей; TPS63020 — buck-boost.
| Пассивка (рез., конд., диод) | См. таблицу ниже | ~47 ₽ |
| **ИТОГО** | | **~2700–3800 ₽** |

**Пассивные компоненты (обвязка ПДА):** детальная привязка refdes, плата и silk — **§ «Пассивка и обвязка LOGIC/POWER»** (сборка v3.0).

| Компонент | Номинал | Кол-во | Назначение |
|-----------|---------|:------:|----------|
| Резистор | 4.7 кОм (допустимо **4.3–4.7 кОм**, напр. 4.4 кОм) | 2 | I2C подтяжка SDA/SCL → 3V3 (одна пара на главной шине ESP↔TCA) |
| Резистор | 220 Ом | 3 | База 2N2222 (вибро + BU03_PWR) + LED красный |
| Резистор | 100 Ом | 1 | LED зелёный |
| Резистор | 1 кОм | 1 | UART TX → DFPlayer RX |
| Резистор | 10 кОм | 2 | **R7:** Shared RST G12 · **R8:** подтяжка затвора P-MOS BU03 |
| Конденс. керам. | 100 нФ | 3 | C1 LoRa · **C2 BU03 (bypass 3.3 V у модуля, не после P-MOS)** · C3 DFPlayer 5V |
| Конденс. керам. | 10 нФ | 1 | C4 debounce RST G12 |
| Диод Шоттки | 1N5819 | 1 | D1 flyback вибромотора (5V BUS) |

> ℹ️ Электролиты НЕ нужны — все модули уже имеют свою обвязку.
> ℹ️ Конденсаторы в EEPROM-картриджах НЕ нужны — TCA9548A модуль уже обвязан.
> ℹ️ Для макета на проводах дополнительной пассивки **не требуется** (кроме проверки подсветки TFT на конкретном модуле).

### Потребление тока (сводка)

> Полная таблица по режимам: **`hardware/POTREBLENIE.pdf`**

| Устройство | Режим | 3.3 V | 5 V | Экв. от АКБ* | ~1000 mAh |
|---|---|---:|---:|---:|---|
| **ПДА** | Ожидание | 120–180 mA | 20–30 mA | 170–200 mA | 5–6 ч |
| **ПДА** | Активная игра | 200–280 mA | 25–35 mA | 280–330 mA | 3–3.5 ч |
| **ПДА** | Пик (запрещено) | до 545 mA | — | до 650 mA | brownout |
| **Аномалия/ЗЗ** | Фон | 100–140 mA | — | 105–150 mA | 6–8 ч |
| **Аномалия/ЗЗ** | UWB + LoRa RX | 200–270 mA | — | 210–285 mA | 4–5 ч |

\* Приведённый ток: `(I3V3×3.3 + I5V×5) / (3.7×0.85)`.

**Ограничения прошивки:** не совмещать **LoRa TX** и **BU03 ranging** на ПДА; **BU03_PWR OFF** вне зоны UWB.

**Аномалия / убежище (полевое устройство, BU03 + LoRa):**

| Компонент | Модель | Цена |
|---|---|:---:|
| Контроллер | ESP32-S3-N16R8 (DevKitC-1) | ~400–600 ₽ |
| UWB | BU03-Kit | ~800–1200 ₽ |
| LoRa | SX1278 Ra-01 433 MHz | ~200 ₽ |
| Зарядка | *(вне текущего scope)* | — |
| DC-DC | TPS63020 → 3.3 V (один модуль) | ~150 ₽ |
| Аккумулятор | Li-Po 3.7 V | донор |
| Корпус | Пластиковый бокс G302 | ~300 ₽ |
| LED зелёный | 5 мм (только **убежище**, G16) | ~10 ₽ |
| **ИТОГО (аномалия)** | | **~1800–2500 ₽** |
| **ИТОГО (убежище)** | + LED | **~1810–2510 ₽** |

**Пассивка (аномалия / убежище):**

| Компонент | Номинал | Кол-во | Назначение |
|-----------|---------|:------:|----------|
| Резистор | 220 Ом | 1 | База 2N2222 BU03_PWR G42 |
| Резистор | 10 кОм | 2 | **R7:** LoRa RST G12 · **R8:** затвор P-MOS |
| Резистор | 100 Ом | 1 | LED зелёный G16 (**только ЗЗ**) |
| Конденс. керам. | 100 нФ | 2 | LoRa VCC, BU03 VCC |
| Конденс. керам. | 10 нФ | 1 | Debounce LoRa RST G12 |
| Транзистор | 2N2222 | 1 | BU03_PWR |
| MOSFET | P-MOS AO3401 | 1 | Ключ 3.3 V на BU03 |

> Пины LoRa и BU03 — **идентичны ПДА**. Питание полевого устройства: **TPS63020 3.3 V**.  
> Полный список: `docs/komponenty.txt`.

> Чипы не расходуются физически! Программатор перезаписывает их — 200 чипов на сезон, используются годами.

### Слоты чипов (через TRRS 3.5мм)

Нужен потому что у дешёвых AT24C32/CAT24C256 одинаковый I2C-адрес. TCA9548A создаёт 5 независимых I2C-шин, каждая выведена на **TRRS-гнездо (PJ-392 PCB-mount)**. Сами чипы EEPROM **не паяются на основную плату** — они в съёмных TRRS-картриджах:

| Канал TCA | Слот ПДА | Тип предмета | Разъём на плате |
|:---:|:---:|---|:---:|
| 0 | Универсальный | Переходники, расходник, админка, магазин, квест, банкомат; **кабель кассы** | TRRS / **J_TRS** (шлейф) |
| 1 | — | *Резерв (не используется на PCB)* | — |
| 2 | Универсальный слот 1 | Любой чип (`TYPE_ARMOR`/`TYPE_ARTIFACT`/др.), тип — поле чипа | TRRS |
| 3 | Универсальный слот 2 | Любой чип | TRRS |
| 4 | — | *Резерв (не используется на PCB)* | — |
| 5 | Универсальный слот 3 | Любой чип | TRRS |
| 6 | Универсальный слот 4 | Любой чип | TRRS |
| 7 | — | *Запасной* | — |

> **Эталон в коде:** `proshivki/common/mux_channels.h` (`MUX_CH_*`). **Актуально с 09.09.2026:** слоты 2/3/5/6 — универсальные, не закреплены за броней/артефактами.
>
> **Все взаимодействия игрок↔терминал — через EEPROM на TCA**, не LoRa/ESP-NOW (радио — только бой: аномалии, RAD, зоны). Два режима на **CH0**:
> - **Handoff:** терминал/программатор пишет заголовок чипа @ `0x00` → игрок вставляет картридж → ПДА читает (`applyChip`).
> - **Кабель:** касса/банкомат/доска — **настольная кассета** + **4-проводный кабель** к **CH0** ПДА; терминал пишет TXN-блок @ `0x80`, ПДА опрашивает ~200 мс (`eeprom_txn.h`).

#### Распиновка TRRS-гнезда (4-pole 3.5мм)

```
  Штекер чипа        Гнездо на ПДА
 ┌───────┐            ┌─────────┐
 │  EEPROM │            │ TCA Ch.N │
 │  DIP-8  │            │  SDAn    │
 ├───────┤            │  SCLn    │
 │ VCC ►──────────► Ring2(3.3V)│
 │ SDA ►──────────► Tip  (SDA) │
 │ SCL ►──────────► Ring1(SCL) │
 │ GND ►──────────► Sleeve(GND)│
 │A0/A1/A2│            └─────────┘
 │  ↓GND  │
 │ WP→GND │
 └───────┘
```

| TRRS-полюс | Сигнал | EEPROM пин | Примечание |
|:---:|---|:---:|---|
| **Tip** | SDA | 5 | I2C данные |
| **Ring 1** | SCL | 6 | I2C тактовый |
| **Ring 2** | 3.3V (VCC) | 8 | Питание чипа |
| **Sleeve** | GND | 4 | Общий минус |

> ⚠️ **Внутри картриджа:** A0, A1, A2 (pin1-3) и WP (pin7) затянуты на GND.
> Все 5 чипов имеют одинаковый I2C-адрес (0x50) — конфликта нет благодаря TCA9548A.

> ⚠️ **Тип разъёма на ПДА:** PJ-392 (**PCB-mount**, вертикальный), 14×8мм футпринт.
> **Тип штекера чипа:** TRRS-вилка 3.5мм (4-pole), паяется непосредственно на EEPROM DIP-8.

### Распиновка EEPROM DIP-8 (вид сверху)

| Ножка | Сигнал | Подключение |
|:-----:|--------|-------------|
| 1–3 | A0, A1, A2 | GND (адрес 0x50 на картридже) |
| 4 | GND | GND BUS |
| 5–6 | SDA, SCL | Канал TCA9548A |
| 7 | WP | **GND** (иначе только чтение) |
| 8 | VCC | **3V3** |

Чипы: **24LC256** (32 КБ) или AT24C32 (4 КБ). WP и питание — критичны при пайке.

### TCA9548A — подключение модуля (стенд и плата)

> **Подтверждено стендом 10.06.2026:** цепочка ESP32 → TCA9548A → 2× EEPROM (каналы CH1+CH2 на стенде, аналогично CH0+CH2 по актуальной карте) — **PASS** на WROOM32 и S3.

#### Архитектура I2C (без лишней обвязки)

```
ESP32 (одна шина I2C) ──► TCA9548A @ 0x70
                            ├── CH0 (SD0/SC0) → универсальный / кабель кассы (TRRS + J_TRS)
                            ├── CH1, CH4 — резерв (не на PCB)
                            └── CH2, CH3, CH5, CH6 → универсальные слоты (любой чип, TRRS)
```

| Принцип | Решение |
|---------|---------|
| Отдельная шина I2C на каждый EEPROM | **НЕ нужна** — достаточно `tcaSelect(channel)` перед обращением к `0x50` |
| Транзисторы / тиристоры на I2C | **НЕ нужны** — TCA сам коммутирует SDA/SCL |
| Конденсаторы у каждого EEPROM | **НЕ нужны** — обвязка на модуле TCA |
| Подтяжки SDA/SCL | **Одна пара** на главной шине (ESP↔TCA); на модуле HW-617 часто уже есть |
| Перезапись EEPROM | **Работает** при WP→GND; тест: запись 8 байт @ `0x0100`, verify OK |

#### Пины ESP32 → TCA (главная шина)

| Плата | SDA | SCL | Когда |
|-------|:---:|:---:|-------|
| **ESP32-S3-N16R8** (целевая ПДА) | **GPIO 9** | **GPIO 8** | Финальная плата |
| **ESP32-WROOM32 DevKit** | **GPIO 21** | **GPIO 22** | Стендовая отладка проводки (⚠️ GPIO 6–11 заняты Flash) |

#### Обязательные соединения на модуле TCA9548A

| Сигнал TCA | Куда | Зачем |
|------------|------|-------|
| VCC | 3.3 V | Питание (не 5 V) |
| GND | GND ESP | Общая земля |
| SDA / SCL | GPIO SDA/SCL ESP | Главная I2C-шина |
| **A0, A1, A2** | **GND** | I2C-адрес мультиплексора **0x70** |
| **RST** | **3.3 V** | Сброс активен по LOW; «в воздухе» — нестабильность |

> На многих модулях **HW-617** подтяжка RST и адресные перемычки уже на плате — проверить визуально.

#### Код выбора канала (эталон)

```cpp
#define TCA_ADDR 0x70
#define EEPROM_ADDR 0x50

bool tcaSelect(uint8_t channel) {
  Wire.beginTransmission(TCA_ADDR);
  Wire.write((uint8_t)(1 << channel));
  return Wire.endTransmission() == 0;
}
// После tcaSelect(ch) — обычный Wire к EEPROM_ADDR
```

Реализация: `test_firmware/EEPROM_TCA_Test/EEPROM_TCA_Test.ino`, `test_firmware/Test_PDA_Hardware.ino` (команда **T8**).

#### Результаты стенда (10.06.2026)

| Плата | I2C | TCA 0x70 | CH0 EEPROM | CH2 EEPROM | Итог |
|-------|-----|:--------:|:----------:|:----------:|:----:|
| ESP32-WROOM32 | G21/G22 | OK | R/W OK | R/W OK | **PASS** |
| ESP32-S3 | G8/G9 | OK | R/W OK | R/W OK | **PASS** |

Тестовая область EEPROM: адрес **`0x0100`** (вне игровых регионов `0x00–0xFF`; см. карту памяти ниже).

#### ⚠️ Безопасность: горячий провод GND

Если при подаче питания **GND греется** — немедленно отключить питание. Это **короткое замыкание** (часто перепутаны VCC/GND, SDA/SCL, или оба EEPROM на одном канале). Чеклист — `test_firmware/EEPROM_TCA_Test/README_EEPROM_TCA_Test.md`.

### KiCad (`hardware/STALKER_PDA_v3.0/`)

- Проекты: `STALKER_PDA_LOGIC.kicad_pro`, `STALKER_PDA_POWER.kicad_pro`
- BOM: `docs/komponenty.txt`
- DRC: 0 ошибок перед заказом; GND-полигон; RF-модули на краю POWER

### Python → C++ (таблица портирования)

| Python (`game_logic.py`) | C++ (ESP32) |
|---|---|
| `json.dump()` / `json.load()` | `NVS` (Preferences library) |
| `socket.sendto()` | `esp_now_send()` |
| `socket.recvfrom()` | `esp_now_recv_cb()` (callback) |
| `struct.pack('<BBhhh')` | `memcpy(&pkt, data, 8)` |
| `pygame` (экран) | `TFT_eSPI` / `Adafruit_GFX` |
| `time.time()` | `millis()` / `micros()` |
| EEPROM json-файл | `Wire.h` + `EEPROM.read()` |
| TCA9548A выбор канала | `Wire.write(1 << channel)` |
| `player.tick()` раз в сек | `if (millis() - lastTick >= 1000)` |

### RSSI — только присутствие (не дистанция)

> **Устарело для метров:** калибровка RSSI ↔ дистанция (5 / 10 / 20 м). **Точные метры — только UWB (BU03).**  
> RSSI остаётся для грубой оценки «сигнал есть / устройство рядом» (детектор, якоря LoRa), не для входа в зону аномалии.

Таблица ниже — **историческая справка** для ESP-NOW (античит «кастрюля» по обрыву пакетов), **не** для прошивки дистанции на плате:

| Дистанция | RSSI (ориентир ESP-NOW) | Примечание |
|:---:|:---:|---|
| 5 м | ~-65 | **Не** использовать как радиус аномалии — см. UWB |
| 10 м | ~-73 | |
| 20 м | ~-81 | |
| 30 м | ~-85 | Порог «сигнал пропал» в `game_logic.py` |

---

## 🃏 ЧАСТЬ 3.5: Формат EEPROM-чипов

> Единственный источник истины для программатора (`programmer.py`) и ПДА (`proshivki/Pda/V1/PDA_ESP32.ino`).

### Строка CONFIG_WRITE (Serial → `Cip_Programmer/Chip_Programmer_ESP32.ino`)

```
CONFIG_WRITE:type=T,sub=S,uses=U,p0=V0,p1=V1,...,p15=V15
```

| Поле | Тип | Описание |
|---|:---:|---|
| `type` | 0-3 | Тип чипа: 0=РАСХОДНИКИ, 1=БРОНЯ, 2=АРТЕФАКТЫ, 3=АДМИНКА |
| `sub` | 0-5 | Подтип (зависит от type) |
| `uses` | 1-99 | Количество (зарядов / штук) |
| `p0..p15` | int16 | Параметры (см. таблицы ниже) |

---

### type=0 — РАСХОДНИКИ

| sub | Название | p0 | p1 | p2..p15 |
|:---:|---|---|---|---|
| `0` | **МГНОВЕННОЕ ЛЕЧЕНИЕ** | Количество HP или % | Режим: `0`=HP, `1`=% MaxHP | — |
| `1` | **АНТИРАДИН** | Снять RAD или % | Режим: `0`=RAD единицы, `1`=% накопленного RAD | — |
| `2` | **РЕГЕНЕРАТОР** | HP/сек | Длительность (сек) | — |
| `3` | **СТИМУЛЯТОР** | % буст | Длительность (сек) | — |
| `4` | **ВОССТАНОВЛЕНИЕ** | Кол-во (ед. или %) | Режим: `0`=единицы, `1`=% от maxUses | PDA выбирает слот (броня/арт); `newUses = min(maxUses, cur + amount)` |
| `5` | **УЛУЧШЕНИЕ** | Доп. прочность (ед.) | % улучш. всех параметров | `newMaxUses=min(250, maxUses+p0)`. **+эффекты:** `⌈val×(1+p1/100)⌉`. **−эффекты (урон):** `floor(val×(1−p1/100))`. |

> 💡 **УЛУЧШЕНИЕ (sub=5, p1):** `p1=10` на артефакте с защитой 20% ХИМИЯ и уроном 5 RAD/тик:
> - Защита: `⌈20 × 1.10⌉ = 22%` ✅ лучше
> - Урон: `floor(5 × 0.90) = 4` RAD/тик ✅ меньше вреда
> - **Правило:** положительное значение → `ceil(v × (1+p/100))`, вредное/отрицательное → `floor(v × (1−p/100))`

> [!IMPORTANT]
> **АБСОЛЮТНЫЙ КЭП ПРОЧНОСТИ = 250 для ЛЮБОГО предмета.**
> При применении чипа УЛУЧШЕНИЕ:
> `newMaxUses = min(250, item.maxUses + p0)` — **ОБЯЗАТЕЛЬНО**.
>
> Пример: предмет имеет `maxUses=60`, чип с `p0=200`.
> `60 + 200 = 260` → но `min(250, 260) = **250**`, не 310.
>
> Это правило действует для ВСЕХ операций с прочностью (ВОССТАНОВЛЕНИЕ, УЛУЧШЕНИЕ).
> Превышение 250 невозможно ни при каких условиях — это защита от бага "бесконечный предмет" (uint8 = 255 → wraparound).

> 💡 **heal_mode (sub=0, p1):** Если `p1=1`, ПДА лечит `round(playerMaxHP × p0 / 100)` HP.
> Если `p1=0` — лечит ровно `p0` единиц HP. Позволяет создать аптечку «на 50% HP» независимо от уровня игрока.

---

### type=1 — БРОНЯ

| Поле | Значение |
|:---:|---|
| `p0` | Защита % тип 0 (ВЗРЫВ) |
| `p1` | Защита % тип 1 (КРОВЬ) |
| `p2` | Защита % тип 2 (ТЕРМО) |
| `p3` | Защита % тип 3 (ЭЛЕКТРО) |
| `p4` | Защита % тип 4 (ХИМИЯ) |
| `p5` | Защита % тип 5 (ПСИ) |
| `p6` | Защита % тип 6 (ГРАВИТ.) |
| `p7` | Защита % RAD |
| `p8` | Реген HP/тик (вычисляется: `HP/мин × интервал / 60`) |
| `p9` | Интервал регена (сек) |
| `p10` | **ДОП. HP** — бонус к MaxHP при надевании брони |
| `p11..p15` | Резерв |

> 💡 **ДОП. HP (p10):** При надевании брони ПДА прибавляет `p10` к `playerMaxHP`.
> При снятии — возвращает. Позволяет создавать «тяжёлую броню» с бонусным запасом здоровья.

---

### type=2 — АРТЕФАКТЫ

| Поле | Значение |
|:---:|---|
| `p0` | Реген HP/тик |
| `p1` | Интервал регена (сек) |
| `p2..p9` | Защита % по типам 0-7 (HP + RAD) |
| `p10..p15` | Урон HP по типам 0-5 (если надет — получаешь урон) |

---

### type=3 — АДМИНКА

| sub | Название | p0 | p1 | Логика ПДА |
|:---:|---|---|---|---|
| `0` | **ВОСКРЕШЕНИЕ** | — | — | Только при `is_dead`: heal, сброс RAD. Любой мастер. **Не** снимает `admit_pending`. |
| `1` | **УПРАВЛЕНИЕ ДЕНЬГАМИ** | Сумма (+/-) руб | — | `player.money += p0`. Отрицательное — снять. |
| `2` | **УРОВЕНЬ/ОПЫТ** | Уровень (+/-) | XP (+/-) | `player.level += p0; player.xp += p1`. Отрицательное — снять. |
| `3` | **ИММУНИТЕТ** | Длительность (мин) | — | `immuneTimer = p0×60000 мс`. Весь входящий урон = 0, пока таймер > 0. |
| `4` | **СБРОС** | — | — | Атомарный откат: `HP=maxHP, RAD=0, все эффекты=0, деньги=стартовые`. Параметры не нужны. |
| `5` | **НЕЙТРАЛИЗАЦИЯ** | — | — | Принудительный переход в `STATE_DEAD` (`is_dead`). Ждёт чип ВОСКРЕШЕНИЕ. |
| `6` | **ДОПУСК В ИГРУ** | Часы (ЧЧ) | Минуты (ММ) | Таймер `p0×3600+p1×60` сек. За 3 мин до конца → предупреждение. Снимает `admit_pending`. Только главный мастер. |

> ~~`2` ПРЕСЕТ (СТАРТ ИГРЫ)~~ — **удалён**. Начальные параметры игрока задаются через ПДА-устройство напрямую.

> [!NOTE]
> **ИММУНИТЕТ vs СТИМУЛЯТОР**: Иммунитет (type=3) — абсолютная защита от *всего* урона. Стимулятор (type=0, sub=3) — временный буст конкретных % защит. Иммунитет для экстренных ситуаций (вход в опасную зону, мед. помощь), стимулятор — для боёвки.

---

### Статус реализации

| Компонент | Статус |
|---|:---:|
| `programmer.py` dev=0 — ЧИП ПРОГРАММАТОР | ✅ Реализован |
| `programmer.py` dev=1 — АНОМАЛИЯ | ✅ Реализован |
| `programmer.py` dev=2 — УБЕЖИЩЕ | ✅ Реализован |
| `programmer.py` dev=3 — ПДА КОНФИГ | ✅ Реализован |
| `proshivki/Cip_Programmer/Chip_Programmer_ESP32.ino` — транспорт Serial→I2C→EEPROM | ✅ Реализован |
| `EEPROM_TCA_Test.ino` — стенд TCA + 2× EEPROM (WROOM32 / S3) | ✅ **PASS 10.06.2026** |
| Железо: TCA9548A + 2× 24LC256 через CH0/CH2 | ✅ **Проверено на стенде** |
| `mux_channels.h` + `eeprom_protocol.h` + `eeprom_txn.h` | ✅ **Протокол v1 в коде** |
| `proshivki/Terminal/Terminal_ESP32/` — терминал (TXN @0x80) | ✅ v1 |
| `programmat_pc/eeprom_txn.py` + `test_eeprom_txn.py` | ✅ |
| `proshivki/Pda/V1/PDA_ESP32.ino` — опрос TXN на CH0 (purchase/bank/quest/admit) | ✅ |
| `proshivki/Pda/V1/PDA_ESP32.ino` — приём `CONFIG:FUNC` / `CONFIG:PRESET` через Serial + NVS | ✅ v2.4+ |
| `proshivki/Pda/V1/PDA_ESP32.ino` — чтение чипов @0x00 + `applyChip()` | ✅ CH0 + слоты 2/3/5/6, пороги уровня |

---

### ПДА КОНФИГ (dev=3 в programmer.py)

Подключается ПДА напрямую по USB к ноутбуку/телефону. Конфиг хранится в **NVS (Preferences)** — сохраняется при выключении.

#### Режим 0: ФУНКЦИИ — вкл/выкл модулей ПДА

```
CONFIG:FUNC:flags=<byte>
```

| Бит | Функция | 1=вкл, 0=выкл |
|:---:|---|:---:|
| 0 | HP система | по умолчанию 1 |
| 1 | Радиация | 1 |
| 2 | Деньги/экономика | 1 |
| 3 | Броня | 1 |
| 4 | Артефакты | 1 |
| 5 | Аномалии | 1 |
| 6 | Уровни/XP | 1 |
| 7 | Чипы-расходники | 1 |

Все включены: `flags=255` (0xFF)

#### Режим 1: ПРЕСЕТ — начальные данные игрока

```
CONFIG:PRESET:maxhp=X,starthp=X,maxrad=X,money=X,lvl=X,xp=X,r0=X,...,r7=X
```

| Поле | Описание | По умолчанию |
|---|---|:---:|
| `maxhp` | Макс. здоровье (внутр. ед., шкала **1000**) | 1000 |
| `starthp` | Стартовое HP | 1000 |
| `maxrad` | Макс. накопление RAD (внутр. **1000** = 100% полосы) | 1000 |
| `money` | Стартовые деньги | **1000** |
| `lvl` | Стартовый уровень | 1 |
| `xp` | Начальный опыт | 0 |
| `r0..r7` | Базовые защиты % (8 типов) | 0 |

**Ответ ПДА:** `OK\n` или `ERR\n`

> [!IMPORTANT]
> ПДА сохраняет конфиг через `Preferences.h` (NVS Flash). При выключении И включении конфиг сохраняется.
> Доп чип для программирования ПДА **не нужен** — только прямое USB-подключение.

### Карта EEPROM 24LC256 (32 КБ)

> **Эталон в коде:** `proshivki/common/eeprom_protocol.h`, TXN-блок — `eeprom_txn.h`.

| Регион | Адрес | Размер | Назначение |
|:---|:---|:---:|---|
| Заголовок чипа | `0x0000..0x0025` | 38 B | type, sub, uses, params[16], CRC16 |
| Расширение чипа | `0x0026..0x007F` | — | Резерв (строки квеста и т.п.) |
| **TXN-блок терминала** | **`0x0080..0x00A3`** | **36 B** | Протокол касса↔ПДА: magic `ST`, state machine, `op_type`, CRC16 |
| После TXN | `0x00A4..0x00FF` | — | Резерв |
| Тест / будущее | `0x0100+` | — | Стенд `EEPROM_TCA_Test` |

#### Заголовок чипа (38 байт)

| Адрес | Поле | Тип | Описание |
|:---:|---|:---:|---|
| `0x00` | `chip_type` | uint8 | Тип (0=Расходники, 1=Броня, 2=Арт, 3=Админ) |
| `0x01` | `chip_sub` | uint8 | Подтип |
| `0x02` | `chip_uses` | uint8 | Прочность (255 = бесконечно) |
| `0x03` | резерв | — | — |
| `0x04..0x23` | `params[0..15]` | 16 × int16 | Параметры (little-endian) |
| `0x24..0x25` | CRC16 | uint16 | Покрывает 0x00..0x23 (CRC-CCITT) |

#### TXN-блок @ `0x80` (кабельные терминалы на CH0)

Используется кассой, банкоматом, доской квестов, столом допуска. ПДА опрашивает **CH0** каждые ~200 мс.

| Поле | Описание |
|---|---|
| `magic` | `0x53 0x54` (`'S''T'`) |
| `state` | IDLE → PENDING → PROCESSING → SUCCESS / FAILED |
| `txn_id` | Уникальный id запроса (дедуп на ПДА) |
| `amount` | RUB: цена / перевод / награда |
| `item_id` | Каталог: товар, квест, код счёта |
| `op_type` | PURCHASE, BANK, QUEST, ADMIT, ATM |
| `flags` | Направление банка, сдача квеста, скидка |
| `balance`, `result`, `paid` | Заполняет ПДА после обработки |

Поток: терминал пишет payload + CRC → `TXN_PENDING` → ПДА видит, ставит `PROCESSING`, применяет handler → `SUCCESS`/`FAILED` → терминал показывает результат → `IDLE`. На шине I2C **один мастер** в момент времени.

### Справочник внешних терминалов (CH0)

> **Эталон кода:** `proshivki/Terminal/Terminal_ESP32/Terminal_ESP32.ino` · `proshivki/common/eeprom_txn.h` · `proshivki/common/eeprom_protocol.h` · `programmat_pc/eeprom_txn.py`  
> **Чеклист реализации:** `docs/PROGRESSION.txt` §9 · **Каналы (без LoRa ПДА↔терминал):** `docs/COMMUNICATION_CANON.md` §3

#### Общая модель

Одна **универсальная плата терминала** (ESP32 + кассета EEPROM 24LC256, опц. TCA9548A). Роль задаётся в NVS (`terminal_role`) и прошивается с ПК через `programmer.py` → `TERMINAL_ROLE:STORE|ATM|QUEST|ADMIT|BANK`. Связь с ПДА — **только EEPROM на CH0** (`mux_channels.h`): handoff-чип @ `0x00` или **кабель** (кассета + 4-провод SDA/SCL/3V3/GND к J_TRS ПДА) + TXN-блок @ `0x80`. **LoRa между ПДА и терминалом не используется** (ни касса, ни банк, ни квесты, ни банкомат).

| Роль NVS | UI (RU)         | `op_type`    | Назначение                              |
| -------- | --------------- | ------------ | --------------------------------------- |
| `STORE`  | Касса           | `0` PURCHASE | Покупка товара                          |
| `BANK`   | Банк            | `1` BANK     | Вклад / снятие (здание банка)           |
| `QUEST`  | Квестовая доска | `2` QUEST    | Выдача и сдача заданий                  |
| `ADMIT`  | Допуск          | `3` ADMIT    | Пропуск в игру (снятие `admit_pending`) |
| `ATM`    | Банкомат        | `4` ATM      | То же, что BANK (полевой UX)            |

**Периферия (будущее):** экран, клавиатура, кнопки — набор зависит от роли; прошивка детектит подключённое и выбирает UI-профиль (см. §9.1 в `PROGRESSION.txt`).

#### TXN-протокол (кабельный режим)

```
Терминал                          EEPROM @0x80                    ПДА (poll ~200 ms)
   │                                    │                              │
   │── build payload + CRC ────────────►│                              │
   │── state = PENDING (последним) ────►│◄── видит PENDING ────────────│
   │                                    │◄── state = PROCESSING ───────│
   │                                    │     handler по op_type        │
   │                                    │◄── SUCCESS/FAILED + balance ──│
   │◄── poll TXN_STATUS / TXN_WAIT ────│                              │
   │── UI результата ──────────────────│                              │
   │── TXN_RESET → IDLE ───────────────►│                              │
```

**Поля блока (36 байт):** `txn_id`, `amount` (RUB), `item_id` (каталог), `op_type`, `flags`, `balance`/`paid`/`result` (заполняет ПДА), `reserved[8]` (префикс quest_id для QUEST).  
**Флаги:** `TXN_FLAG_BANK_DEPOSIT` (0x02) — вклад, иначе снятие; `TXN_FLAG_QUEST_COMPLETE` (0x04) — сдача квеста, иначе принятие; `TXN_FLAG_DISCOUNT` (0x01) — ПДА применил скидку ранга.

**Serial терминала (115200):** `STALKER_WHO`, `TERMINAL_ROLE[:ROLE]`, `TERMINAL_CFG[:limit_purchase=…,limit_withdraw=…,limit_deposit=…]`, `CONFIG_READ`, `TXN_START:…`, `TXN_STATUS`, `TXN_WAIT`, `TXN_RESET`, каталог QUEST (`QUEST_*`).

**ПК-программатор:** вкладка «Терминал» — роль + лимиты кассы/банкомата + опционально задания QUEST. `.ino` заливается **один раз**; дальше только USB. Сверх лимита терминал отвечает `ERROR:LIMIT:max=N`.

---

#### STORE — касса (торговец)

|                   |                                                                                                                                                                                                             |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Доступ игрока** | Ур. **2+** (`LVL_STORE`); блокировка при `admit_pending` / смерти                                                                                                                                           |
| **op**            | `TXN_OP_PURCHASE` (0), флаги не используются                                                                                                                                                                |
| **Поток**         | Мастер/торговец на терминале выбирает позицию каталога → `TXN_START:amount=<цена>,item=<id>` → игрок подключает ПДА кабелем → ПДА списывает RUB (со **скидкой ранга**) → терминал показывает SUCCESS/FAILED |
| **ПДА**           | `txnHandlePurchase`: проверка уровня и баланса, `calcShopPrice()`, запись `paid`/`balance`, уведомление «ПОКУПКА #id»                                                                                       |
| **Мастер**        | Лимит покупки (`limit_purchase`) пишется с ПК в NVS терминала; `TXN_START` сверх лимита — отказ |
| **Игрок**         | Подключает ПДА, ждёт списания; на TFT — полосы и журнал (цифры RUB в notify допустимы)                                                                                                                      |
| **Handoff**       | Альтернатива: чип «магазин» @ `0x00` → вставка в CH0 → `applyChip()` (магазин/квест пока TXN @0x80; расходник/админ — заголовок) |
| **Будущее UI**    | Экран терминала: сетка товаров, сумма, номер позиции; опц. подтверждение на стороне торговца до `PENDING`                                                                                                   |
| **Реализация**    | ✅ протокол + `Terminal_ESP32` + TXN на ПДА; ⏳ полевой UI, каталог на терминале                                                                                                                            |

---

#### BANK — банк (здание)

|                   |                                                                                                                                                             |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Доступ игрока** | Ур. **3+** (`LVL_ATM`); те же блокировки                                                                                                                    |
| **op**            | `TXN_OP_BANK` (1)                                                                                                                                           |
| **Флаги**         | `TXN_FLAG_BANK_DEPOSIT` — **вклад** (списание с наличных ПДА); без флага — **снятие** (зачисление на ПДА)                                                   |
| **Поток**         | Игрок у стойки → мастер/банкир вводит сумму и направление → `TXN_START:amount=…,flags=2` (вклад) или `flags=0` (снятие) → кабель → ПДА меняет `playerMoney` |
| **ПДА**           | `txnHandleBank`: deposit требует достаточно наличных; withdraw всегда зачисляет                                                                             |
| **Мастер**        | Лимиты снятия/вклада (`limit_withdraw` / `limit_deposit`) в NVS терминала; журнал операций на ПК (будущее) |
| **Будущее**       | Счёт `item_id` как код операции/кассы; интеграция с модулем статистики (`PROGRESSION.txt` §10)                                                              |
| **Реализация**    | ✅ TXN + прошивка ПДА v2.4+; ⏳ терминал на полигоне                                                                                                          |

---

#### ATM — банкомат (полевой)

|                     |                                                                                                                |
| ------------------- | -------------------------------------------------------------------------------------------------------------- |
| **Доступ**          | Ур. **3+** — тот же порог, что BANK                                                                            |
| **op**              | `TXN_OP_ATM` (4) — на ПДА обрабатывается **идентично** `TXN_OP_BANK` (`txnHandleBank`)                         |
| **Отличие от BANK** | Только роль NVS и UX: автомат на локации, без стойки мастера; `Terminal_ESP32` по умолчанию подставляет `op=4` |
| **LoRa**            | **Не планируется.** Переводы и снятие/вклад — только EEPROM/CH0 (согласовано с `COMMUNICATION_CANON.md`)       |
| **Перевод ПДА↔ПДА** | Отдельная механика (ур.3+), не роль ATM; handoff-чип или будущий протокол — вне текущего TXN                   |
| **Будущее UI**      | Экран: «Внести / Снять», ввод суммы цифрами, индикация ожидания ПДА                                            |
| **Реализация**      | ✅ op=4 + лимиты NVS с программатора; ⏳ корпус банкомата, клавиатура суммы |

---

#### QUEST — доска заданий

**Данные на терминале / ПК (полная карточка):** порядковый **ID**, **имя** (короткое), **полный текст** задания, **награда RUB** (и XP = RUB при сдаче), **режим выдачи**, **таймаут**. Хранится в БД события; мастер прошивает каталог на ESP доски по USB («Прошить доску»).

**Режимы выдачи** (терминал QUEST ведёт слоты в NVS, на кассете — только висящие карточки):

| Режим | Что видит игрок | Если не сдал |
| ----- | --------------- | ------------ |
| **timeout** (по умолчанию) | Эксклюзивно: взял — с доски пропало | Через `timeout_min` (по умолчанию **120 мин**) снова висит для других |
| **oneshot** | Эксклюзивно: взял — пропало | **Больше не появляется**, даже по таймауту |
| **shared** | Висит для всех | Несколько ПДА могут взять одно и то же |

Скрытые (`hidden=1`) — только ур. 20+ на ПДА. Полный текст остаётся на физической доске / в ПК; в EEPROM → ПДА: **id + название** (до 24 символов).

**Каталог EEPROM (кассета CH0):** listed overlay @ `0x0100` (магия `QS`), полный бэкап @ `0x0800`. Заявка ПДА «взять/сдать» @ `0x00A4` (магия `TQ`). Дальше обычный QUEST TXN @ `0x80`.

**В EEPROM → ПДА при TXN:** `item_id`, префикс `quest_id` (до 8 символов), при сдаче `amount` = награда RUB, флаг `TXN_FLAG_QUEST_COMPLETE`.

| Тип задания          | Где видно                                        | Доступ                           |
| -------------------- | ------------------------------------------------ | -------------------------------- |
| **Обычные**          | Публичная доска                                  | С ур. **2** (`LVL_STORE`)        |
| **Скрытые (личные)** | Доска / режим QUEST `hidden=1`                   | Ур. **20+** (`LVL_HIDDEN_QUEST`) |

**Поток (кабель CH0):**

1. Мастер вбивает карточки в модуле «Доска заданий» и жмёт «Прошить доску» (USB → `QUEST_ADD` / `QUEST_CATALOG_COMMIT`).
2. Игрок подключает ПДА к доске. На экране оверлей со списком висящих заданий.
3. **ОК** — заявка `TQ` (взять). Терминал, если слот свободен, пишет QUEST TXN. Название попадает в `activeTasks[]` (NVS).
4. Сдача: меню «Задания» → **ОК** на активном, пока кабель в доске. Награда RUB + XP.
5. Если timeout-задание не сдано за отведённое время — снова в списке. Oneshot не возвращается. Shared не исчезает.

**Serial доски:** `QUEST_CATALOG_CLEAR`, `QUEST_ADD:id=…,rub=…,mode=timeout\|oneshot\|shared,timeout_min=120,hidden=0,title=…` (`title` последним), `QUEST_CATALOG_COMMIT`, `QUEST_CLAIMS`, `QUEST_DUMP`.

Handoff-чип CH0 (выдача/сдача одной карточкой) по-прежнему работает параллельно.

---

#### ADMIT — пропуск (допуск в игру)

|                         |                                                                                                                                                                       |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **op**                  | `TXN_OP_ADMIT` (3); `amount=0`, деньги не участвуют                                                                                                                   |
| **Поток**               | Игрок вставляет/подключает ПДА → мастер на терминале **активирует** допуск → `TXN_START` (без суммы) → ПДА снимает `admit_pending`, вызывает `completeRegistration()` |
| **ПДА**                 | Пока `admit_pending` — вся игровая логика заблокирована; после SUCCESS — полный доступ (если не `is_dead`)                                                            |
| **Альтернатива**        | Админ-чип `CmdSub.ADMIT` (type=3 sub=6) — главный мастер, без терминала                                                                                               |
| **Мастер**              | Только персонал; терминал с ролью `ADMIT` на входе полигона                                                                                                           |
| **Отличие от REGISTER** | ADMIT — **сессионный** допуск (каждое включение ПДА); не привязывает UID к игроку в БД                                                                                |
| **Реализация**          | ✅ TXN + обработчик в `PDA_ESP32.ino`; ⏳ терминал на КПП, UI «Активировать допуск»                                                                                     |

---

#### REGISTER — регистрация

|                         |                                                                                                                                                                           |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Назначение**          | Привязка **PDA UID ↔ player_id** события, имя/позывной/группа в БД (`PROGRESSION.txt` §10.2)                                                                              |
| **Где**                 | **ПК мастера** — модуль «Участники» (`CONFIG:REGISTER:name=…`); опционально **тот же корпус** терминала в режиме REGISTER (отдельная роль или подрежим ADMIT — **TBD**) |
| **Порядок на полигоне** | 1) REGISTER (имя в NVS, БД) → 2) ADMIT (допуск в смену) — шаги могут быть **раздельными** или объединены в один сценарий на КПП                                           |
| **ПДА**                 | `needs_registration` / экран «РЕГИСТРАЦИЯ»; `playerRegistered` + `playerName` в NVS; достижение A02                                                                                      |
| **Не путать**           | Регистрация ≠ допуск: можно быть зарегистрированным, но с `admit_pending` до чипа/терминала/`CONFIG:ADMIT`                                                                         |
| **Реализация**          | ✅ USB `CONFIG:REGISTER` / `CONFIG:ADMIT` + модуль ПК (`stalker_app`); ⏳ полевой терминал REGISTER                                                                                        |

---

#### Мастерская (ремонт / крафт) — handoff, не TXN-роль

Отдельной роли в `Terminal_ESP32` нет. Мастерская использует **handoff-чипы** @ `0x00` (`upgrade`, расходники) или программатор. TXN-роли — только таблица выше.

---

#### Сводка: кто что делает

| Действие                      | Мастер / торговец            | Игрок                       |
| ----------------------------- | ---------------------------- | --------------------------- |
| Выбор роли терминала          | `programmer.py` → ПРОШИТЬ    | —                           |
| Касса: цена, позиция          | `TXN_START`                  | Подключить ПДА              |
| Банк/ATM: сумма, вклад/снятие | `TXN_START` + flags          | Подключить ПДА              |
| Квест: карточка на доске      | Ввод ID, имя, текст, награда | Читать доску, кабель, сдача |
| Пропуск                       | Активация ADMIT              | Подключить ПДА              |
| Регистрация                   | ПК: имя + UID в БД           | Экран регистрации на ПДА    |

> ⚠️ **Known Gap:** per-type интервалы урона артефакта в UI (`programmer.py`) пока **не все** записываются в EEPROM — см. `ROADMAP.md` Фаза 5.

---

## 🎮 ЧАСТЬ 4: Игровая механика Player

### Шкала HP / RAD (batch 3)

| Параметр | Значение |
|---|---|
| **Внутренняя единица** | **1000** для HP, RAD, урона в пакетах, медкитов, выброса и т.д. |
| **Эталон правил** | `game_logic.STAT_SCALE = 1000`; `max_health`, `max_rad` по умолчанию 1000 |
| **Экран TFT** | Полосы HP/RAD — **только % заполнения**; **без цифр** HP/RAD на главной |
| **Сопротивления** | На экране — **только проценты** |
| **Стартовые деньги** | **1000** RUB (`money`, `cfgStartMoney`) |
| **XP / уровни** | **TBD** — отдельный проход; таблица в `PROGRESSION.txt` §2 не менялась в batch 3 |
| **Шрамы Зоны** | Накопление от урона → +% сопр.; кап 50% — **OK**; точная формула порога — **на ревью** |

Стадии RAD (по % полосы): 0–49% скрытая; 50–99% болезнь (−1% max HP/мин); 100% → зомби.

### Иерархия ролей на полигоне

> Игровой обзор для мастеров и дизайнеров. Подробнее для игроков: `ИГРОВОЙ_СПРАВОЧНИК.md` §1.1.  
> Полевые **устройства** (аномалии, убежища, терминалы) — не роли людей.

| Роль | Субъект | Ключевые полномочия | Реализация |
|---|---|---|:---:|
| **Игрок-сталкер** | Участник с ПДА | Стандартный цикл: HP/RAD, чипы, прогрессия | ✅ Спека + база прошивки |
| **Главный мастер** | Организатор сессии | **ДОПУСК В ИГРУ** при каждом вкл. ПДА; выброс; ранги; полная админка | ✅ |
| **Мастер поля** | Помощник на полигоне | **Воскрешение** мёртвого сталкера; часть админ-чипов. **Без** допуска при вкл. ПДА | ✅ |
| **Роль-актор** | Игрок (контроллер, мутант, особый NPC) | Особые цели и механики, влияние на других игроков | ❌ Запланировано |
| **Игрок-зомби** | Сталкер в статусе зомби | Тот же ПДА: только «Сдаться»; входящие TXN без notify, кроме broadcast | ✅ Фаза 2 |

**Допуск vs воскрешение (канон, обзор дизайна batch 1):**

| Действие | Кто | Условие | Экран |
|---|---|---|---|
| **ДОПУСК В ИГРУ** | Только главный мастер | Каждое включение ПДА (`admit_pending`) | «ОЖИДАНИЕ ДОПУСКА» |
| **ВОСКРЕШЕНИЕ** | Любой мастер поля | `is_dead` после агонии / сдачи (не зомби) | Баннер «СВЯЗЬ ПОТЕРЯНА» поверх UI |

Переход в **зомби** (RAD 100%, выброс, пси) — отдельная роль, не обычное воскрешение. См. §4 «Состояния после смерти».

> **Реализовано 17.06.2026:** `CmdSub.ADMIT` (допуск) и `CmdSub.REVIVE` (воскрешение) — раздельно в `protocol.py`, `game_logic.py`.

### Зелёная зона (таймаут)

`is_in_safe_zone()` = **true**, если с момента последнего пакета `Emitter.BASE + Msg.SAFE_ZONE` прошло **менее 5 секунд**.  
Если пакетов нет **более 5 секунд** — игрок считается вне убежища, LED G16 гаснет.

### Радиация — три стадии

| Уровень RAD (% полосы) | Стадия | Эффект |
|:---:|---|---|
| 0 – 49% | Скрытое | Нет эффекта, шкала растёт |
| 50 – 99% | Лучевая болезнь | **−1% max HP каждые 60 секунд** (10 ед. при шкале 1000) |
| 100% | Смертельная доза | **Смерть → Превращение в ЗОМБИ** |

### Состояния блокировки (допуск и воскрешение)

> **Два независимых уровня** (17.06.2026, уточнение):
> - **Блокировка системы** — `admit_pending`, снимается `type=3, sub=6` / `CmdSub.ADMIT` (главный мастер). Полный lock экрана.
> - **Смерть в игре** — `is_dead`, снимается `type=3, sub=0` / `CmdSub.REVIVE` (любой мастер). Игровое состояние внутри разблокированной системы.

> **Каждое включение ПДА** → `admit_pending = true` (не в NVS).  
> **Система ПДА** блокируется только при `admit_pending`.  
> **Бой и эффекты** блокируются при `admit_pending` **или** `is_dead`.

| Состояние | Экран | Кто снимает | Чип / команда |
|---|---|---|---|
| Включение / перезапуск (жив) | «ОЖИДАНИЕ ДОПУСКА» (полный lock) | **Главный мастер** | ДОПУСК В ИГРУ (`sub=6`, `CmdSub.ADMIT`) |
| Смерть в игре (`is_dead`) | UI + баннер «СВЯЗЬ ПОТЕРЯНА» | **Любой мастер поля** | ВОСКРЕШЕНИЕ (`sub=0`, `CmdSub.REVIVE`) + heal |
| Вкл. после смерти | Сначала допуск (lock), затем UI + баннер смерти | Главный, затем поле | Оба чипа по очереди |

Выключение ПДА в процессе игры = выход → при следующем вкл. снова **допуск** (только главный мастер).  
Античит от обхода аномалий через power-off.

### Состояния после смерти

- **Агония** (`in_agony`): обычный урон довёл HP до **0**. Это **не** `is_dead` и не «СВЯЗЬ ПОТЕРЯНА». Выход: аптечка / лечение ЗЗ / пакет `MSG_HEAL` → снова жив; повторный урон или **«Сдаться»** → обычная смерть. Выброс / пси / RAD 100% **минуют** агонию и сразу дают роль зомби.
- **Обычная смерть** (повторный удар в агонии, **Сдаться** из меню п.4 или с экрана агонии/зомби): `is_dead`, баннер «СВЯЗЬ ПОТЕРЯНА» → **воскрешение** мастером поля.
- **Роль зомби** (`is_zombie`): RAD 100%, гибель от **выброса**, смертельный **пси контроллера** — экран **«ВЫ ЗОМБИ»**. Игрок **не** открывает меню, чипы, магазин: единственное действие — **«Сдаться»** (двойное **ОК**). Входящие переводы, задания и начисления **применяются**, но **без уведомлений**. Исключение — общие оповещения мастера (`CONFIG:BROADCAST`, радио, предупреждения выброса). Не `CmdSub.REVIVE`. См. `ИГРОВОЙ_СПРАВОЧНИК.md` §12–§13.

### Слоты ПДА

| Канал TCA | Тип | Допустимый чип / режим |
|:---:|---|---|
| `CH1`, `CH4` | Резерв | — |
| `CH0` | Универсальный | Расходник, админка; handoff-чипы; **кабель** кассы/банка/квестов (TXN @ `0x80`) |
| `CH2`, `CH3`, `CH5`, `CH6` | Универсальные слоты | Любой чип (`TYPE_ARMOR`/`TYPE_ARTIFACT`/др.) — тип определяется полем чипа |

> Магазин / квесты / банкомат — **EEPROM на CH0** (чип или кабель), не LoRa. Эталон: `mux_channels.h`. Справочник ролей и TXN: **Часть 3.5** «Справочник внешних терминалов (CH0)»; чеклист: `docs/PROGRESSION.txt` §9.

### Инвалидность кадров (I-Frame)

Урон от одного MAC не чаще **0.9 с**. Разные MAC — независимые таймеры.

### Ключевые методы Player (`game_logic.py`)

```python
player.on_power_on()              # каждое вкл. → admit_pending=True
player.register_player(name)      # ПК/EEPROM → registered, player_name
player.needs_registration         # not registered
player.grant_session_admit()      # CmdSub.ADMIT (после регистрации)
player.grant_revive()             # CmdSub.REVIVE (не при is_zombie)
player.is_system_locked()         # needs_registration or admit_pending
player.is_combat_locked()         # lock or is_dead or is_zombie (агония — нет)
player.is_in_agony()              # HP=0 после обычного урона, ещё не is_dead
player.cheat_shield_count         # античит экранирования (без HP)
player.apply_damage(...)
```

---

## ☢️ ЧАСТЬ 5: Механика Выброса (Emission)

### Алгоритм фаз

```
Сервер → EMISSION(val1=таймер_сек, val2=длительность_сек)

━━━━ ФАЗА 1: ОБРАТНЫЙ ОТСЧЁТ (emission_timer > 0) ━━━━
  emission_timer -= 1 каждую секунду; пороги 3600 / 1800 / 900 / 300 с

━━━━ ФАЗА 2: ACTIVE STRIKE (emission_timer == 0) ━━━━
  Каждые 10 секунд, если НЕ в Safe Zone:
    apply_damage(250, DMG_PSY) + apply_radiation(250)   # шкала 1000
  Если is_in_safe_zone() (пакет SAFE_ZONE < 5 с назад) → урона нет

━━━━ ФАЗА 3: КОНЕЦ (emission_duration == 0) ━━━━
  emission_timer = -1; уведомление «ВЫБРОС ОКОНЧЕН»
```

### Выживаемость без защиты

| Время | HP (от 1000) | RAD (% полосы) | Итог |
|:---:|:---:|:---:|---|
| 0 сек | 1000 | 0% | — |
| 10 сек | 750 | 25% | — |
| 20 сек | 500 | 50% | Лучевая болезнь |
| 40 сек | 💀 | 100% | Смерть |

### UI-индикаторы

| Место | Показ |
|---|---|
| Нижняя строка | `"ВЫБРОС!"` красным |
| Рамка экрана | Мигает красным каждые 0.5 с |
| LED G16 | Гаснет вне убежища |

---

## 🌀 ЧАСТЬ 7: Аномалии и Убежища

### Конфиг аномалии (anomaly_memory.json)

| Поле | Тип | Описание |
|---|:---:|---|
| `cat_idx` | uint8 | 0=Аномалия, 1=Убежище |
| `sub_idx` | uint8 | Тип урона (0–7, см. DmgType) |
| `dmg_amount` | int | Начальный урон |
| `dmg_max` | int | Максимальный урон |
| `dmg_step` | int | Рост урона в минуту |
| `frequency` | int | Перезарядка (секунды) |
| `recharge_mode` | 0/1 | 0=Реактивный, 1=Автономный |
| `multi_target` | 0/1/2 | 0=Один, 1=Мина, 2=Лужа |
| `eruption_time` | int | Время лужи (сек) |
| `radius` | int | Радиус в метрах |

| Поле Убежища | Описание |
|---|---|
| `sz_regen_hp` | HP за тик |
| `sz_cleanse_rad` | Снятие RAD за тик |
| `sz_psy_shield` | Пси-защита (%) |
| `sz_interval` | Интервал тика (сек) |
| `uwb_zone_id` | Фиксированный UWB ID убежища (tag, 0–10); прошивается один раз на точку |
| `uwb_entry_m` | Порог дистанции UWB для входа (метры) |

### Режимы аномалии

| Режим | Поведение |
|---|---|
| **Реактивный (Засада)** | Спит, пока нет цели. Атакует при появлении → перезарядка |
| **Автономный (Гейзер)** | Бьёт строго по таймеру, вне зависимости от цели |
| **ОДИН ИГРОК** | Ближайший по **UWB** (`AT+DISTANCE`) |
| **ПОДРЫВ (МИНА)** | AoE: бьёт всех в радиусе одновременно, уходит в КД |
| **ВЫБРОС (ЛУЖА)** | Рассылает урон N секунд всем входящим в зону |

### Нарастающий урон

```
Каждую минуту: текущий_урон = min(dmg_max, текущий_урон + dmg_step)

Если dmg_step = 0 → урон всегда = dmg_amount (стабильная аномалия)
```

### UWB: две схемы ролей (аномалия vs убежище)

> ESP-NOW — игровые пакеты (DAMAGE, SAFE_ZONE, ACK). UWB — **метры**. Порог по дистанции **до** действия, не после.

#### Аномалия (`cat=0`) — anchor на точке, tag на ПДА

| Устройство | BU03 Role | UWB ID |
|------------|-----------|--------|
| Аномалия | **1** anchor | Фиксированный ID точки |
| ПДА в зоне | **0** tag | **Динамический слот** 0–10 (выдаёт аномалия) |

1. ПДА шлёт heartbeat / `ZONE_HELLO` (ESP-NOW).
2. Аномалия выдаёт `ZONE_ASSIGN slot=N` → ПДА: `AT+SETCFG=N,0,1,1`.
3. Аномалия опрашивает `AT+DISTANCE` пока игрок в зоне.
4. `dist < cfgRadius` → удар по `cfgTarget` (ОДИН / МИНА / ЛУЖА) → DAMAGE по **MAC** из таблицы слотов.
5. Выход из зоны → `SLOT_RELEASE`, сброс слота, `BU03_PWR` OFF.

**Правила:** одна аномалия в одной точке; уникальный слот на игрока; привязка **slot ↔ ESP-NOW MAC** в NVS. Две аномалии рядом — отложено (ROADMAP).

#### Убежище / зелёная зона (`cat=1`) — **роли наоборот**, фиксированный ID зоны

| Устройство | BU03 Role | UWB ID |
|------------|-----------|--------|
| Убежище | **0** tag | **Один фиксированный ID зоны** (прошивается при настройке точки, хранится в NVS) |
| ПДА у входа | **1** anchor | Без динамических слотов |

1. ПДА по ESP-NOW видит beacon убежища (`Msg.SAFE_ZONE`) → включает BU03, переключает роль: `AT+SETCFG=0,1,1,1` (anchor).
2. ПДА опрашивает `AT+DISTANCE` до **фиксированного tag ID** этой зоны.
3. `dist < порог входа` → ESP-NOW **`ENTRY_OK`** на ESP убежища → разрешение (хил, защита, флаг «в зоне»).
4. Вне зоны / потеря beacon → ПДА возвращает BU03 в режим ожидания, питание OFF.

**Слоты не нужны** — у каждого убежища свой постоянный UWB ID (региональный ID точки). Несколько убежищ на полигоне: ID 1, 2, 3… (задаётся в программаторе); ПДА меряет ID **той** зоны, чей beacon видит.

#### Пакеты ESP-NOW (зона, черновик)

| Пакет | Направление | Смысл |
|-------|-------------|--------|
| `ZONE_HELLO` | ПДА → аномалия | Вхожу в зону |
| `ZONE_ASSIGN` | Аномалия → ПДА | Слот N (только аномалия) |
| `SLOT_READY` | ПДА → аномалия | BU03 принял слот |
| `SLOT_RELEASE` | ПДА → аномалия | Вышел из зоны |
| `ENTRY_OK` | ПДА → убежище | Дистанция OK, пускать |

---

## 🕵️ ЧАСТЬ 8: Античит

### «Кастрюля» (`game_logic.py` / ESP-NOW)

> На **железе** вход в зону и дистанция — **UWB**. Ниже — логика по **обрыву ESP-NOW-пакетов**, не RSSI-калибровка метров.

```
Проблема: Игрок экранирует сигнал аномалии.
Симптом:  пакеты шли стабильно → резко пропали без затухания.

Алгоритм (player.tick() каждую секунду):
1. Получаем пакет урона → in_anomaly_zone = True
2. Пауза > 3.5 сек без пакетов при недавнем сильном RSSI → cheat_shield_count += 1
   (без урона HP; мастер смотрит счётчик через admin/serial — будущее)
```

| Параметр | Значение |
|---|:---:|
| Таймаут без пакетов | 3.5 сек |
| Штраф HP | **Нет** — только `cheat_shield_count` |

### Калибровка на полигоне

1. **UWB:** `AT+SETDEV` на каждой аномалии и ЗЗ (см. §3 BU03)  
2. **RSSI:** при необходимости — только пороги «слышим / не слышим» для детектора и LoRa-якорей, **не** метры

## 🎖️ ЧАСТЬ 8.5: Прогрессия — уровни, ранги, достижения (v4)

> **Полный справочник (62 достижения, таблица 100 уровней):** `docs/PROGRESSION.txt`  
> Аналогия: **Fallout 4** + **Warframe Mastery** + **Enlisted** (ступени достижений).

### Слоты TCA (итог, актуализировано 09.09.2026)

| CH | Назначение |
|:---:|---|
| 0 | **Универсальный** — расходник, админка, магазин, квест, банкомат (чип → ПДА читает) |
| 2, 3, 5, 6 | Универсальные слоты (броня/артефакты/др. — по типу чипа) |
| 1, 4 | Резерв |

### Уровни (1–100)

- Растут **автоматически** по XP (бит 6 `cfgFuncFlags`).
- При level-up: **+RUB награда** (не списание). Правила — в `PROGRESSION.txt` §2.
- **Формула XP на шаг (L → L+1):** `90+L×32` (1–9) → `200+(L−9)×8` (10–39) → `420+(L−39)×4` (40–69) → `460+(L−69)×3` (70–99). Суммарно до ур. 100 ≈ **41 625 XP** при активной игре **6–7 игровых дней**.
- **Квесты:** при сдаче **XP = RUB** награда чипа (`rub_reward`); вехи G01–G07 — бонус поверх.
- **Титул ур. 100:** **ХРОНИКЁР ЗОНЫ** (отдельно от ранга «ЛЕГЕНДА» на ур. 80).

### Регистрация игрока (ПК / EEPROM)

1. **Первое вкл.** — экран «РЕГИСТРАЦИЯ», без взаимодействия с миром.
2. ПДА на **ПК** через EEPROM в слоте (как касса): `programmer.py` → `CONFIG:REGISTER:name=...` (**Фаза 1.5** / прошивка **Фаза 5**) → `player_name`, `registered` в NVS.
3. После регистрации — **допуск** главного мастера (`CmdSub.ADMIT`).
4. **Конец сезона** — чтение NVS на ПК, экспорт / личный чип.

Инструктаж → ур. 2, магазин, универсальный слот (артефакт), обычные квесты, `reg_complete`.

### Ранги (мастер подтверждает; **без XP**)

Ранг требует **больше**, чем минимальный уровень: доп. **квесты** и/или **достижения**. Мастер подтверждает (`CONFIG:RANK_CONFIRM`) только когда **все** условия выполнены. Код: `game_logic.RANK_PREREQ_ACH`.

| Инд. | Мин.ур. | Название | Доп. условия | RUB | Скидка |
|:---:|:---:|---|---|:---:|:---:|
| 0 | 1 | НОВИЧОК | — | — | 0% |
| 1 | 10 | БРОДЯГА | `reg_complete` | 800 | 3% |
| 2 | 25 | СТАЛКЕР | — | 1500 | 6% |
| 3 | 40 | ВЕТЕРАН | `quest_1` (≥1 квест) | 2500 | 10% |
| 4 | 60 | МАСТЕР | — | 4000 | 14% |
| 5 | 80 | ЛЕГЕНДА | `quest_15` (≥15 квестов) | 6000 | 18% |

> **XP / уровни:** формулы и таблица 1–100 — `docs/PROGRESSION.txt` §2.

Подтверждение ранга мастером — через **UI ПДА / терминал** (`CONFIG:RANK_CONFIRM` в прошивке). **Отдельный EEPROM-чип «РАНГ» не реализован** (в планах).

`цена = витрина × (1 − скидка_ранга/100)`

### UI — всегда доступен

Страницы 0–2 (главная, инвентарь, сопротивления) **не блокируются** уровнем.

### Доступ к механикам (CH0 / экипировка)

| Ур. | Открывается |
|:---:|---|
| 1 | HP, RAD, **CH0** расходник |
| **2** | Регистрация, **магазин**, **универсальный слот** (артефакт), **обычные квесты** |
| **3** | **Банкомат**, **ПДА↔ПДА** |
| 5 | **универсальный слот** броня |
| 7 | **универсальный слот** артефакт 2 |
| 10 | **универсальный слот** артефакт 3 |
| 12 | Арена |
| **20** | **Скрытые квесты** (флаг `hidden` в чипе) |
| **25** | **ДЕТЕКТОР АНОМАЛИЙ** (мигание LED при пакете аномалии рядом, ESP-NOW) |
| **50** | **Глобальные сообщения** *(будущее, платно — TBD)* |
| 100 | Титул «Хроникёр Зоны» |

### Детектор аномалий (не «ESP»)

Название в UI: **ДЕТЕКТОР АНОМАЛИЙ**. Уровень **25+**.  
**Простое правило:** аномалия **рядом** (пакет ESP-NOW от `Emitter.ANOMALY` в зоне действия) → **красный LED мигает**; гаснет через **~2.5 с** без сигнала.  
Без ярусов дистанции (120/80/50 м). `game_logic.py`: любой пакет DAMAGE/RADIATION от аномалии при ур. ≥25.

### Достижения

62 шт. в `PROGRESSION.txt` (категории A–O): ступени разведки 1/5/10/20…, экономика, квесты обычные/скрытые, арена, **Марафонец** (4+ ч сессии, без привязки к «ночи»).

Экран: `ДОСТИЖЕНИЕ: … +XP +RUB` (~5 сек). **Основной источник XP — сдача квестов (XP = RUB чипа);** достижения — бонус поверх.

**Не даёт XP:** зелёная зона (только вехи-достижения), расходники, подтверждение ранга.

---

## 💾 ЧАСТЬ 9: Сохранение данных

### JSON (`game_logic.py`) → NVS (ESP32)

| JSON-поле | NVS namespace | NVS-ключ | Тип ESP32 |
|---|:---:|:---:|:---:|
| `health` | `"player"` | `"hp"` | int16 |
| `max_health` | | `"hp_max"` | int16 |
| `radiation` | | `"rad"` | int16 |
| `max_rad` | | `"rad_max"` | int16 |
| `money` | | `"money"` | int32 |
| `xp` | | `"xp"` | int32 |
| `level` | | `"lvl"` | int8 |
| `rank` | | `"rank"` | int8 | Подтверждённый ранг (0–5) |
| `achievements` | | `"ach"` | uint32 | Битовая маска достижений |
| `registered` | | `"reg"` | bool | Прошёл инструктаж |
| `death_counter` | | `"deaths"` | int16 |
| `innate_resistance[i]` | | `"res_0"…"res_6"` | int8 | HP-типы 0-6 (0-50%) |
| `damage_taken_acc[i]` | | `"acc_0"…"acc_6"` | int16 | аккумулятор HP-типов 0-6 |
| `rad_resistance` | | `"rad_res"` | int8 | защита от RAD (отдельно) |
| `rad_accumulator` | | `"rad_acc"` | int16 | накопитель дозы RAD |

> NVS ESP32 выдерживает ~100,000 циклов записи. `save_state()` вызывается при каждом изменении HP, смерти, XP.

---

## 🖥️ ЧАСТЬ 10: UI и Управление

### Страницы TFT (ILI9341 **320×240**) — **3 верхнего уровня**

| Стр. | Содержимое | Всегда доступна |
|:---:|---|:---:|
| **0** | **Главная:** HP, RAD (**полосы %**, без цифр HP/RAD), уровень, деньги; **журнал уведомлений — 2 строки** | ✅ |
| **1** | **Инвентарь:** броня, артефакты, баланс, расходник | ✅ |
| **2** | **Меню** — список подразделов (см. ниже); не отдельные top-level страницы | ✅ |

**Подразделы страницы 2 «Меню»** (навигация **ВНИЗ** по списку, **ОК** — войти / детали):

| № | Подраздел | Содержимое |
|:---:|---|---|
| 0 | **Сопротивления** | 8 типов (7 HP + радиация): **только %** на экране |
| 1 | **Активные квесты** | `ActiveTask` из CH0; ОК — описание / сдача |
| 2 | **Выполненные достижения** | Список разблокированных за сезон |
| 3 | **Сдаться / самоубийство** | Двойное **ОК** → `is_dead`, баннер **СВЯЗЬ ПОТЕРЯНА** |

> **TBD (расширение меню):** счётчик защиты / таймер зонной защиты, статистика смертей — добавляются в тот же список без новых top-level страниц.

> **Прошивка (`PDA_ESP32.ino`):** 3 страницы верхнего уровня + подменю (сопротивления / квесты / достижения / сдаться / настройки / UWB).

Навигация: **ВПРАВО** — цикл страниц 0→1→2→0; **ВНИЗ** — уведомления (стр. 0) / курсор инвентаря (стр. 1) / пункт меню (стр. 2); **ESC** — назад из подраздела; **ОК** — расходник (стр. 0) / слот (стр. 1) / подменю (стр. 2).

### Управление страницами и комбинации

- **Кнопка ВПРАВО** — переключает страницы (**0 → 1 → 2 → 0**).
- **Кнопка ВНИЗ** — листает уведомления (стр. 0) / курсор инвентаря (стр. 1) / пункты меню и элементы внутри подраздела (стр. 2).
- **Кнопка ОК** — использовать расходник / извлечь предмет / войти в подраздел меню / прочитать детали квеста или достижения / подтвердить сдачу (двойное ОК в пункте «Сдаться»).

### LED (ПДА)

| LED | GPIO | Поведение |
|---|---|---|
| **Красный** | G15 | **Вспышка** в момент получения урона (~250–1000 мс) |
| **Зелёный** | G16 | **Постоянно ON**, пока `isInSafeZone()` (пакет SAFE_ZONE < 5 с) |
| **Красный (мигание)** | G15 | Ур. **≥25** (детектор) **и** пакет аномалии рядом (ESP-NOW) — чередование с вспышкой урона |

### Физические ключи питания (PWR / VIB) и тумблер BL

> **Текущая сборка:** ключа **SND нет**. DFPlayer питается вместе с ПДА. Громкость 0–30 — страница **НАСТРОЙКИ** на экране (ВНИЗ тише, ОК громче; 0 = без звука). G47/G48 не используются.
>
> **PWR, VIB** — разрыв линии питания на корпусе (если VIB установлен). Прошивка **не отключает** вибро программно ключом.  
> **BL** — трёхпозиционный тумблер на корпусе: контакты **ON→G37**, **AUTO→G38**, общий **GND** (INPUT_PULLUP). Прошивка читает положение для UI; **питание LED TFT не управляется** (LED → 3V3). См. § «Подсветка TFT» выше.

| Ключ / тумблер | Расположение в цепи | Эффект |
|---|---|---|
| **PWR** | В разрыве **Li-Po → DC-DC** | Полное отключение ПДА (механический разрыв питания) |
| **SND** | **Нет на текущей сборке** | Громкость только в настройках на экране |
| **VIB** | В разрыве **5V BUS → M+ вибромотора** (если ключ есть) | Вибро: ветка обесточена при ВЫКЛ; при ВКЛ ESP импульсирует G21 → 2N2222 |
| **BL** | **Не в цепи LED** — только **G37/G38** | Подсветка TFT **всегда ON** при питании; тумблер задаёт режим в UI (OFF/AUTO/ON), без PWM/обрыва LED |

GPIO **G37**, **G38** — тумблер BL. **G47/G48 не используются.**

### Вибрация (политика batch 4)

При **физическом ключе VIB = ВКЛ** (питание на ветке) короткий импульс G21 (≈100–500 мс) **только** на события из whitelist (таблица ниже). Прошивка **не отключает** вибро программно — только физический ключ.

| Тип события | Примеры |
|---|---|
| `damage` | Урон от аномалии, выброса, PvP |
| `death` | HP = 0, самоубийство из меню |
| `radiation` | Накопление RAD |
| `level_up` | Повышение уровня |
| `achievement` / `rank_ready` | Достижения, «ранг доступен» |
| `revive` | Воскрешение мастером (`CmdSub.REVIVE`) |
| `info` | Предупреждения выброса, чип вставлен, задание принято |

**Без вибро:** `money`, `xp`, покупки/магазин, `heal` (аптечка), `antirad`, `error`, `rank_confirmed`.

Звук — таблица треков DFPlayer; громкость задаётся в **НАСТРОЙКАХ** на ПДА. Ключа SND нет.

### Уведомления

- Хранятся в `player.notifications` (макс 5 последних)
- Поля: `text`, `time`, `type` (`achievement`, `rank_ready`, `rank_confirmed`, `level_up`, `info`), `priority`
- Формат достижения: `ДОСТИЖЕНИЕ: <название> +XP +RUB`
- Формат ранга (до подтверждения мастером): `РАНГ ДОСТУПЕН: <название>`
- Время жизни одного: 10 секунд; приоритетные (достижение/ранг) показываются первыми
- **Страница 0:** постоянная зона **2 строки** для журнала уведомлений; **ВНИЗ** — пролистать историю
- При выбросе нижняя строка транзиентных событий показывает `"ВЫБРОС!"`

### Активные задания (квесты CH0)

Минимальная модель (`game_logic.ActiveTask`): `id`, `title`, `short_desc`, `status` (`active` / `completed`).
Чип квеста в CH0: `modifiers.quest`, `quest_id`, `title`, `short_desc`; сдача — `complete: true`.
Отображение — подраздел **«Активные квесты»** в меню (стр. 2), не отдельная top-level страница.


---

## Заливка, стенды, диагностика

Arduino IDE, платы, UWB, ограничения боевых `.ino`: **`proshivki/FLASHING.md`**.

Стенды (`test_firmware/`): Test_PDA_Hardware, EEPROM_TCA_Test, LoRa_Test_*, BU03_*. Не ставить на полигон вместо боевых скетчей.

План работ: **`ROADMAP.md`**. Каналы: **`docs/COMMUNICATION_CANON.md`**.
