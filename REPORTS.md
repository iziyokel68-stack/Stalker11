# 📜 Лог разработки и отчёты (REPORTS)

Журнал изменений проекта. Спецификация — `MASTER_SPECIFICATION.md`, план — `ROADMAP.md`, свод каналов — `docs/COMMUNICATION_CANON.md`.

---

## Программа мастера — внутри окна, LoRa/EEPROM, карта
*Дата: 9 сентября 2026*

### Суть

Пять правок консоли мастера:

1. Программатор — Tk-фрейм в том же окне (`modules/programmer.py`), не pygame-subprocess.
2. Выброс в UI — **минуты** (`timer_min` / `duration_min`; в LoRa-пакете по-прежнему секунды int16).
3. Громкость DFPlayer 0–30 общая для радио и уведомлений (`CONFIG:VOLUME` / LoRa VOLUME). Радио шлёт только трек. Клавиша SND — питание, не уровень.
4. Мастер ↔ ПДА: только LoRa по № регистрации или общий EEPROM + одно USB-устройство на ПК/телефоне мастера. Не кабель к каждому ПДА. Имя — чип `type=3 sub=7` @0x26.
5. Карта: скриншот-подложка и маяки с известными координатами (`map_meta`, `map_beacons`).

CHIP_BOX на `LORA_TX` отвечает `ERROR:NO_LORA` (нужен Мастер-Пульт).

### Файлы

`stalker_app/` · `programmat_pc/serial_link.py` · `programmat_pc/config_builder.py` · `proshivki/Pda/V1/PDA_ESP32/PDA_ESP32.ino` · `proshivki/Cip_Programmer/Chip_Programmer_ESP32.ino`

---

## Программа мастера — консоль STALKER App
*Дата: 9 сентября 2026*

### Суть

`stalker_app/` из оболочки с заглушками стал рабочим местом мастера: одно окно, боковое меню, база события, USB Serial.

| Модуль | Что умеет |
|--------|-----------|
| Событие | Открыть / создать / закрыть, импорт-экспорт `.db` на USB |
| Участники | Список, поиск, группы, `CONFIG:REGISTER` / `CONFIG:ADMIT` / UID |
| Программатор | Прежний `programmer.py` (COM отпускается на время окна) |
| Оповещения | Журнал + USB broadcast / выброс / радио (LoRa пульта — позже) |
| Задания | Каталог квестов события |
| Статистика | Снимок вручную или `CONFIG_READ` |
| Команды | Допуск, воскрешение, UID |
| Карта | Заглушка схемы полигона |

`SerialLink` вынесен в `programmat_pc/serial_link.py`. Прошивка ПДА: имя в NVS, экран «РЕГИСТРАЦИЯ», USB-команды ADMIT/REVIVE/BROADCAST.

### Файлы

`stalker_app/` · `programmat_pc/serial_link.py` · `proshivki/Pda/V1/PDA_ESP32/PDA_ESP32.ino` · `ROADMAP.md` Фаза 4 · `docs/PROGRESSION.txt` §10–§11

---

## TFT M024 — полный экран на собранной плате
*Дата: 17 августа 2026*

### Суть

На собранном ПДА v3 модуль **M024-240320** заполняет всё стекло **320×240** только так:

1. `tft.begin()`
2. `tft.setRotation(1)` — логический размер GFX 320×240
3. сырая команда **MADCTL `0x36` = `0x88`** (MY \| BGR), **без бита MV**
4. цвета RGB565: красный/синий меняются (`TFT_BGR_SWAP`)

Adafruit `setRotation()` одна даёт мёртвую полосу ~80 px (снизу при 1/3, справа при 0/2). `0xC8` заполняет экран, но зеркалит; рабочее значение — **`0x88`**.

### Эталон

| Файл | Роль |
|------|------|
| `proshivki/common/tft_panel.h` | канон: `tftApplyPanel()`, `TFT_MADCTL_PANEL` |
| `test_firmware/TFT_Standalone_Test/` | проверка на плате (остальная периферия заглушена) |
| `test_firmware/Test_PDA_Hardware/` | guided-тест — тот же `tftApplyPanel` |
| `proshivki/Pda/V1/PDA_ESP32/` | боевая прошивка ПДА |

Вызов: сразу после `tft.begin()` → `tftApplyPanel(tft)`. Не использовать rotation 0/2 для UI.

---

## Документация — статус фаз 3–4 (июнь 2026)
*Дата: 26 июня 2026*

### Суть

Синхронизация `ROADMAP.md`, `MASTER_SPECIFICATION.md`, `COMMUNICATION_CANON.md` с фактическим статусом:

| Тема | Было | Стало |
|------|------|-------|
| **PCB v3.0** | ⏸ ожидание ревью | ✅ ревью специалистом завершено |
| **Подсветка TFT** | бэклог AUTO / rev v3.1+ | снят; LED → 3V3, BL только UI (G37/G38) |
| **BU03 стенд** | частично | ✅ `Test_PDA_Hardware`, AT-тесты, PDA v2.7 UWB |
| **ЗЗ UWB** | ⏳ | ✅ на стенде; `ENTRY_OK` в боевой прошивке — ⏳ |
| **Аномалия UWB** | ⏳ | ⏳ без изменений (`ZONE_ASSIGN` не в коде) |
| **Список пайки** | без антенны | + **антенна LoRa 433 МГц** (пружина на Ra-01) |

### Файлы

`ROADMAP.md` · `MASTER_SPECIFICATION.md` · `docs/COMMUNICATION_CANON.md` · `hardware/komponenty.txt`

---

## Аудит GPIO и подсветки TFT — PCB v3
*Дата: 26 июня 2026*

### Суть

Завершён аудит распиновки **ESP32-S3-N16R8** на плате ПДА v3 и реальной схемы подсветки TFT. Зафиксировано: **26 занятых GPIO**, **свободных пинов для новой периферии нет**; подсветка **не управляется** из прошивки на текущем железе.

### GPIO (сводка)

| Категория | GPIO |
|---|---|
| **Используются (26)** | 1, 2, 4–18, 21, 37–42, 47, 48 |
| **Запрещены** | 0, 3, 19, 20, 35, 36, 45, 46 (strapping / USB / flash) |
| **Внутренние N16R8** | 26–34 (octal flash/PSRAM, не на DevKit) |
| **Не существуют** | 22–25 |
| **U0TXD/U0RXD** | 43, 44 — на штырьках DevKit, **на PCB не разведены**; **не** под TFT_BL |

### Подсветка — решение

| Факт | Деталь |
|------|--------|
| **Схема v3** | Пин **LED** J_TFT → **3V3** (всегда ON при питании) |
| **Тумблер BL** | ON→**G37**, AUTO→**G38**, общий GND; только **INPUT_PULLUP** |
| **Прошивка** | Может читать положение BL для UI; **не может** выключить/затемнить LED |
| **AUTO (idle dim)** | Нужен GPIO на линии LED — **невозможно** на v3 |
| **История** | Тест-скетчи: `TFT_BL` на **G21** → на v3 G21 = вибро |

### Варианты на будущее

1. LED на 3V3; BL — только логика UI (**текущее**).
2. Механика: OFF обрывает питание LED; AUTO физически всегда ON.
3. **PCB v3.1+:** LED через N-MOS от GPIO (нужен свободный пин — сейчас нет без tradeoff).
4. **Не G43/G44** для TFT_BL — потеря USB flash/логов.

### Согласованность

`hardware/schematic.html`, `MASTER_SPECIFICATION.md` §3, `proshivki/Pda/V1/PDA_ESP32/PDA_ESP32.ino` — **26 функциональных пинов**. Исправлены формулировки, где подразумевалось программное управление подсветкой без оговорки об ограничении железа.

### Файлы

`MASTER_SPECIFICATION.md` · `hardware/schematic.html` · `ROADMAP.md` · `hardware/BOARD_SPEC_v3_FOR_REVIEW.md`

---

## Cashier v1.1 — ESP32-WROOM-32, тест с ПДА CH1
*Дата: 25 июня 2026*

### Суть

Прошивка кассы адаптирована под **ESP32-WROOM-32 DevKit** (I2C GPIO 21/22); профиль **ESP32-C3** сохранён. Добавлены Serial-команды `I2C_SCAN` / `EEPROM_PING`. Процедура сквозного теста (кассета → кабель → ПДА CH1, CH2 = броня) — `test_firmware/Cashier_Test/README_Cashier_Test.md`.

### Файлы

`proshivki/Cashier/Cashier_ESP32.ino` · `test_firmware/Cashier_Test/README_Cashier_Test.md`

---

## Документация — физические ключи PWR / SND / VIB / BL
*Дата: 25 июня 2026*

### Суть

Вкл/выкл **питания ПДА**, **звука**, **вибро** и **подсветки TFT** — только **физическими ключами** в разрыве линии питания на корпусе. **Не GPIO ESP** (G37/G38/G47/G48 свободны). Прошивка не отключает звук/вибро/подсветку программно.

| Ключ | Цепь |
|------|------|
| **PWR** | Разрыв Li-Po → DC-DC |
| **SND** | Разрыв 5V → DFPlayer |
| **VIB** | Разрыв 5V → вибромотор (импульсы G21 только при поданном питании) |
| **BL** | Разрыв 3V3 → LED подсветки TFT (3-поз. ON/OFF/ON на корпусе) |

### Файлы

`MASTER_SPECIFICATION.md` · `hardware/BOARD_SPEC_v3_FOR_REVIEW.md` · `hardware/komponenty.txt` · `hardware/schematic.html` · `ИГРОВОЙ_СПРАВОЧНИК.md` · `ROADMAP.md` · `proshivki/Pda/V1/PDA_ESP32.ino` · `test_firmware/Test_PDA_Hardware/Test_PDA_Hardware.ino`

---

## User review batch 4 — детектор, регистрация, зомби, античит
*Дата: 22 июня 2026*

### Цель

Применить правки обзора дизайна: упростить детектор; §11 регистрация через ПК/EEPROM; состояние зомби; античит без HP → `cheat_shield_count`; арена §7.5; свод бэклога в ROADMAP.

### Что сделано

| Тема | Решение |
|------|---------|
| **§7.1 Детектор** | Аномалия **рядом** = пакет ESP-NOW → LED; без ярусов 120/80/50 м |
| **§7.4 / §7.5** | Смерть от игрока = меню «Сдаться»; арена ур.12+ и L01–L04 |
| **§11 Регистрация** | Экран «РЕГИСТРАЦИЯ» → ПК/EEPROM → допуск; `register_player()` в `game_logic.py` |
| **§13 Зомби** | `is_zombie`, экран «ВЫ ЗОМБИ», выход только «Сдаться» |
| **§14 Античит** | `cheat_shield_count++`, без `ANTICHEAT_PENALTY_HP` |
| **ROADMAP** | Сводная таблица бэклога; Фаза 5 регистрация/зомби/античит |
| **Код** | `game_logic.py`, комментарии `PDA_ESP32.ino`, `programmer.py` |

### Файлы

`ИГРОВОЙ_СПРАВОЧНИК.md` v1.4 · `MASTER_SPECIFICATION.md` · `ROADMAP.md` · `game_logic.py` · `PDA_ESP32.ino` · `programmer.py`

---

## EEPROM TXN-протокол, касса, карта каналов TCA
*Дата: 22 июня 2026*

### Цель

Унифицировать все взаимодействия игрок↔терминал через EEPROM на TCA9548A (без LoRa/ESP-NOW между ПДА и кассой/банком/квестами). Ввести кабельную архитектуру: настольная кассета + 4-провод к CH1 ПДА, общий EEPROM, TXN-блок @ `0x80`. Зафиксировать новую карту каналов и подготовить прошивку кассы + тесты.

### Что сделано

| Тема | Решение |
|------|---------|
| **Карта TCA** | `mux_channels.h`: CH0/CH3 — резерв; **CH1** — универсальный + кабель кассы; **CH2** — броня; **CH4–6** — артефакты 1–3 |
| **Единый EEPROM** | `eeprom_protocol.h` — карта памяти 24LC256: заголовок чипа @ `0x00`, TXN @ `0x80` |
| **TXN v1** | `eeprom_txn.h` — state machine (IDLE→PENDING→PROCESSING→SUCCESS/FAILED), `op_type`: PURCHASE, BANK, QUEST, ADMIT, ATM |
| **Прошивка кассы** | `Cashier_ESP32.ino` v1.0 — Serial `TXN_START` / `TXN_STATUS` / `TXN_WAIT` / `TXN_RESET`; guard **TXN_BUSY** при незавершённой транзакции |
| **ПДА** | `PDA_ESP32.ino` — опрос CH1 каждые ~200 мс, обработчики purchase/bank/quest; ADMIT — stub (`NOT_IMPLEMENTED`) |
| **Программатор PC** | `programmer.py` — парсинг TXN-ответов; `eeprom_txn.py` — зеркало протокола на Python |
| **Документация** | `MASTER_SPECIFICATION.md`, `ROADMAP.md`, `PROGRESSION.txt` — каналы, TXN-поток, терминалы без радио |

**Два режима на CH1:**
- **Handoff** — терминал/программатор пишет заголовок @ `0x00` → игрок вставляет картридж → ПДА читает (`applyChip`, Фаза 5).
- **Кабель** — кассета на столе + 4-wire (SDA, SCL, 3V3, GND) к CH1; терминал пишет TXN @ `0x80`; на шине **один мастер** в момент времени.

### Файлы

| Файл | Назначение |
|------|------------|
| `proshivki/common/mux_channels.h` | Эталон каналов TCA9548A |
| `proshivki/common/eeprom_protocol.h` | Карта памяти EEPROM, имена op_type |
| `proshivki/common/eeprom_txn.h` | TXN-блок @ `0x80`, CRC16, builders, state machine |
| `proshivki/Cashier/Cashier_ESP32.ino` | Прошивка терминала кассы (ESP32-C3, I2C к кассете) |
| `proshivki/Pda/V1/PDA_ESP32.ino` | Опрос TXN на CH1, handlers purchase/bank/quest |
| `programmat_pc/eeprom_txn.py` | Python-реализация TXN + `pda_process_*` |
| `programmat_pc/programmer.py` | TXN-команды и разбор `OK:TXN_*` / `ERROR:TXN_BUSY` |
| `programmat_pc/test_eeprom_txn.py` | Unit-тесты протокола |
| `MASTER_SPECIFICATION.md` | § слоты, EEPROM map, TXN-поток |
| `ROADMAP.md` | Фаза 5.7 — терминалы через CH1 |
| `hardware/PROGRESSION.txt`, `PROGRESSION.txt` | Синхронизация схемы слотов |

### Схема каналов TCA9548A

| CH | Назначение | Разъём |
|:---:|---|:---:|
| **0** | Резерв (не на PCB) | — |
| **1** | Универсальный: расходники, админка, handoff-чипы; **кабель кассы/банка/квестов** (TXN @ `0x80`) | TRRS + **J_TRS** |
| **2** | Броня (`TYPE_ARMOR`) | TRRS |
| **3** | Резерв (не на PCB) | — |
| **4** | Артефакт 1 (`TYPE_ARTIFACT`, ур. 2+) | TRRS |
| **5** | Артефакт 2 (ур. 7+) | TRRS |
| **6** | Артефакт 3 (ур. 10+) | TRRS |
| **7** | Запасной | — |

> Эталон: `proshivki/common/mux_channels.h`. Заменяет старую схему CH0=броня, CH1–3=арты, CH4=универсальный (см. запись v6.8 от 12.06.2026).

### Протокол EEPROM (TXN @ `0x80`)

```
Регион: 0x0080..0x00A3 (36 B), magic "ST", version 1, CRC16 CCITT

Поток:
  1. Терминал: payload + CRC → state = PENDING (последним)
  2. ПДА (~200 мс): PENDING → PROCESSING → handler → SUCCESS/FAILED + balance/paid/result
  3. Терминал: poll до terminal state → UI → RESET → IDLE

op_type: 0=PURCHASE  1=BANK  2=QUEST  3=ADMIT  4=ATM
flags:   DISCOUNT | BANK_DEPOSIT | QUEST_COMPLETE
result:  OK, INSUFFICIENT_FUNDS, LEVEL_TOO_LOW, SYSTEM_LOCKED, BAD_CRC, QUEST_*, NOT_IMPLEMENTED, …
```

Заголовок игрового чипа — отдельно @ `0x00..0x25` (type, sub, uses, params, CRC); не пересекается с TXN.

### Что осталось

| Задача | Статус |
|--------|--------|
| `applyChip()` на железе — handoff-чипы @ `0x00` | ⏳ Фаза 5 |
| Полный инвентарь CH2 + CH4–6 + подтверждение использования чипа | ⏳ Фаза 5 |
| Терминалы банкомат / доска квестов / мастерская (прошивки) | ⏳ Фаза 5.7 |
| `TXN_OP_ADMIT` — обработка на ПДА | stub |
| Стенд: касса + кабель 4-wire → ПДА CH1, end-to-end покупка | ⏳ |
| HMAC/CRC подпись чипов @ `0x2C` | ⏳ |
| Ревью PCB v3.0 | ⏸ |

### Тестирование

| Тест | Результат |
|------|-----------|
| `programmat_pc/test_eeprom_txn.py` | pack/unpack, purchase (success/insufficient/level/locked), discount, bank deposit/withdraw, quest accept/duplicate |
| `Cashier_ESP32.ino` + Serial | `TXN_START`, `TXN_STATUS`, `TXN_WAIT`, `TXN_RESET`, `ERROR:TXN_BUSY` |
| Железо: TCA + кассета + кабель → ПДА | **не прогонялось** — следующий шаг стенда (#6 в тест-парке) |

**Синхронизированы:** `mux_channels.h`, `eeprom_protocol.h`, `eeprom_txn.h`, `Cashier_ESP32.ino`, `PDA_ESP32.ino`, `eeprom_txn.py`, `programmer.py`, `MASTER_SPECIFICATION.md`, `ROADMAP.md`, `PROGRESSION.txt`, `BOARD_SPEC_v3_FOR_REVIEW.md`, `ИГРОВОЙ_СПРАВОЧНИК.md` §6.

---

## Обзор дизайна batch 4 — меню сдачи, вибро whitelist, XP-формула
*Дата: 17 июня 2026*

**Контекст:** самоубийство из меню, селективная вибрация, документирование прогрессии XP.

**Зафиксировано:**

| Тема | Решение |
|------|---------|
| **Самоубийство** | Пункт **4** меню (стр. 2): «Сдаться» → двойное **ОК** → `Player.surrender()` → **СВЯЗЬ ПОТЕРЯНА** |
| **Убрано** | Удержание ВНИЗ + ОК 5 сек |
| **Вибро** | Whitelist: урон, смерть, RAD, level_up, достижение, rank_ready, revive, info (выброс и пр.) |
| **Без вибро** | money, xp, heal, antirad, error, rank_confirmed, покупки |
| **XP / уровни** | Формула §2 `PROGRESSION.txt`; таблица ключевых уровней + примерные пути в `ИГРОВОЙ_СПРАВОЧНИК.md` §10.1 |
| **grant_achievement** | Без изменений; полный ребаланс XP достижений — TBD |

**Синхронизированы:** `game_logic.py`, `PDA_ESP32.ino` (комментарии), `ИГРОВОЙ_СПРАВОЧНИК.md`, `MASTER_SPECIFICATION.md` Часть 10, `ROADMAP.md`, `PROGRESSION.txt`.

---

## Обзор дизайна batch 3 — шкала статов, UI, ранги, деньги
*Дата: 17 июня 2026*

**Контекст:** внутренняя шкала HP/RAD, отображение на TFT, стартовый капитал, уточнение рангов.

**Зафиксировано:**

| Тема | Решение |
|------|---------|
| **Шкала HP/RAD** | Внутр. **1000** ед. (урон, медкиты, пакеты, пресет) — не 100 и не 10 000 |
| **Экран** | Полосы HP/RAD — **только %**; **без цифр** HP/RAD на главной; сопротивления — **%** |
| **Деньги** | Старт **1000** RUB |
| **XP / уровни** | **TBD** — отдельный проход; таблицы не менялись |
| **Ранги** | Строже уровня: квесты + достижения; мастер подтверждает при **всех** условиях |
| **Без изменений** | Счётчик смертей; стадии RAD 0–49 / 50–99 / 100%; innate resistance (формула — на ревью) |

**Синхронизированы:** `ИГРОВОЙ_СПРАВОЧНИК.md` §4, §8, §10; `MASTER_SPECIFICATION.md` Часть 4, 5, 8.5, 9–10; `ROADMAP.md` Фаза 5; `PROGRESSION.txt` (корень + `hardware/`); `game_logic.py`, `programmer.py`, `PDA_ESP32.ino` (пресеты).

**Код:** `STAT_SCALE=1000`, выброс 250/250 за тик, античит 300 HP, болезнь −10 HP/мин; `RANK_PREREQ_ACH` без изменений логики.

**Known gap:** старые JSON-сейвы с HP=100 отобразят ~10% полосы до ресета/нового пресета. Прошивка: полное меню 3 стр. и шкала 1000 — Фаза 5.

---

## Обзор дизайна batch 2 — PDA UI, LED, вибро
*Дата: 17 июня 2026*

**Контекст:** уточнение экрана ПДА, индикации и тактильной обратной связи.

**Зафиксировано:**

| Тема | Решение |
|------|---------|
| **Страницы** | **3 top-level:** 0 Главная, 1 Инвентарь, 2 Меню (подразделы внутри) |
| **Главная (0)** | HP, RAD, уровень, деньги + **журнал уведомлений 2 строки** |
| **Меню (2)** | Список: Сопротивления → Активные квесты → Достижения → **Сдаться**; ВНИЗ / ОК (двойное ОК для сдачи) |
| **TBD в меню** | Счётчик защиты, статистика — позже, без новых top-level страниц |
| **Красный LED** | Вспышка **в момент урона** |
| **Зелёный LED** | **Постоянно** в зелёной зоне |
| **Красный мигает** | Детектор (ур. 25+) + аномалия ≤80 м |
| **Вибро** | При VIBRO ВКЛ — **whitelist** важных событий (не деньги/покупки/XP) |
| **Без изменений** | Звук (DFPlayer), 5 слотов чипов |

**Синхронизированы:** `ИГРОВОЙ_СПРАВОЧНИК.md` (§3), `MASTER_SPECIFICATION.md` (Часть 10), `ROADMAP.md` (Фаза 2), `programmat_pc/game_logic.py`.

**Прошивка:** `PDA_ESP32.ino` — комментарии и целевая структура; полное подменю + LED blink детектора — **Фаза 5**.

**Known gap (без изменений):** допуск / воскрешение — один чип в коде; разделение по роли мастера — как в batch 1.

---

## Обзор дизайна batch 1 — иерархия ролей
*Дата: 17 июня 2026*

**Контекст:** итеративный обзор игрового дизайна (секция 1: тип игры + роли).

**Зафиксировано:**

| Тема | Решение |
|------|---------|
| Тип игры | Без изменений: полевой LARP / выживание, S.T.A.L.K.E.R., физический ПДА |
| **Игрок-сталкер** | Основной участник со стандартным циклом |
| **Главный мастер** | Допуск в игру при **каждом** включении ПДА; выброс; ранги; полная админка |
| **Мастер поля** | Воскрешение мёртвых; часть админ-чипов; **без** допуска при вкл. ПДА |
| **Роль-актор** | Игрок (контроллер, мутант, особый NPC) с особыми целями — **запланировано** |
| **Игрок-зомби** | Отдельная **роль человека** с ПДА при смерти от RAD/выброса/пси — **запланировано** |
| Допуск vs воскрешение | Два разных действия: допуск (главный, power-on) / воскрешение (полевой мастер, `is_dead`) |

**Синхронизированы:** `ИГРОВОЙ_СПРАВОЧНИК.md` (§1, §1.1, §7.6, §11), `MASTER_SPECIFICATION.md` (Часть 4, 8.5), `ROADMAP.md` (Фаза 1, 5).

**Known gap:** проверка «кто мастер» на ПДА не реализована — разграничение только тем, кому выдан чип допуска vs воскрешения. Прошивка: полный `applyChip()` для sub=0/sub=6 — ⏳ Фаза 5.

---

## Разделение допуска и воскрешения в коде
*Дата: 17 июня 2026*

**Исправление:** объединение «ДОПУСК МАСТЕРА» и `CmdSub.REVIVE` отменено.

| | ДОПУСК В ИГРУ | ВОСКРЕШЕНИЕ |
|---|---|---|
| **Кто** | Главный мастер | Любой мастер поля |
| **EEPROM** | `type=3, sub=6` | `type=3, sub=0` |
| **Сеть** | `CmdSub.ADMIT` (=7) | `CmdSub.REVIVE` (=1) |
| **Снимает** | `admit_pending` | `is_dead` + heal |

~~Игра: `not admit_pending and not is_dead`.~~ **Уточнено** (см. запись ниже).  
Реализовано: `protocol.py`, `game_logic.py`, `programmer.py`, `PDA_ESP32.ino`, документация.

---

## Допуск = система, смерть = игровое состояние
*Дата: 17 июня 2026*

**Коррекция дизайна:** `is_dead` больше **не** блокирует всю систему ПДА.

| Уровень | Флаг | Эффект |
|---|---|---|
| **Блокировка системы** | `admit_pending` | Полный lock экрана «ОЖИДАНИЕ ДОПУСКА» |
| **Смерть в игре** | `is_dead` | Баннер «СВЯЗЬ ПОТЕРЯНА» поверх UI; бой/тик заблокированы |

- `is_system_locked()` / `is_play_locked()` → только `admit_pending`
- `is_combat_locked()` → `admit_pending or is_dead` (урон, лечение, тик)
- После допуска ПДА работает даже при смерти (инвентарь, страницы, уведомления)
- Воскрешение по-прежнему требует `not admit_pending`

Реализовано: `game_logic.py`, `PDA_ESP32.ino`, `ИГРОВОЙ_СПРАВОЧНИК.md` §11, `MASTER_SPECIFICATION.md` Часть 4.

---

## Допуск мастера при каждом включении ПДА (устарело)
*Дата: 17 июня 2026*

**~~Решение~~:** ~~чип «ДОПУСК МАСТЕРА» (`type=3, sub=0`, `CmdSub.REVIVE`)~~ — **заменено** разделением ADMIT/REVIVE (см. запись выше).

> **Уточнение (batch 1, тот же день):** допуск при power-on и воскрешение мёртвого — **разные** действия. **Реализовано** в коде 17.06.2026.

| Триггер | Поведение | Мастер (дизайн batch 1) |
|---------|-----------|-------------------------|
| Первое включение | Экран «ОЖИДАНИЕ ДОПУСКА» → чип мастера | **Главный** — допуск |
| Выключил ПДА в игре | При вкл. снова `admit_pending` → к мастеру | **Главный** — допуск |
| Смерть в игре | «СВЯЗЬ ПОТЕРЯНА» → чип + heal | **Любой мастер поля** — воскрешение |

`admit_pending` **не** пишется в NVS — всегда `true` при boot. `is_dead` сохраняется.  
Реализовано: `game_logic.py`, `PDA_ESP32.ino` (мин.), документация.

---

## ✅ Схема и железо — ФИНАЛЬНОЕ СОГЛАСОВАНИЕ
*Дата: 17 июня 2026*

**Статус: этап проектирования железа завершён.**

| Решение | Статус |
|---------|--------|
| **`hardware/schematic.html`** | **УТВЕРЖДЕНА** — эталон логики и обвязки |
| Состав модулей, пины, питание, обвязка | **Согласовано полностью** |
| Открытые вопросы по железу | **Нет** |

**Зафиксировано:**

- Схема прошла итоговую проверку: трассировка **5V** (ESP, DFPlayer, вибро), **3V3 BUS**, **TFT**, **вибро + D1**, **BU03 POWER KEY** (G42, P-MOS, C2), **LoRa / BU03** — без замечаний.
- Канон железа — **`MASTER_SPECIFICATION.md` v6.0** + **`hardware/schematic.html`** + **`hardware/STALKER_PDA_v3.0/`** (KiCad).
- **Сборка платы (KiCad v3.0)** — на **паузе**: макет, по ощущениям, **собран**, передан на **повторную вычитку специалистом по PCB** (спокойная финальная проверка разводки).
- **Прошивка** — **не в работе** до полного согласования **логики и механик** (`MASTER_SPECIFICATION.md` / `ИГРОВОЙ_СПРАВОЧНИК.md`). О железной прошивке возвращаемся после этого.

---

## Статус сборки платы и очерёдность работ
*Дата: 17 июня 2026 · **снимок на эту дату**; актуально — запись «Документация — статус фаз 3–4 (июнь 2026)» выше.*

| Направление | Статус |
|-------------|--------|
| Схема и состав железа | ✅ Утверждено |
| KiCad LOGIC + POWER v3.0 | ⏸ **Приостановлено** — ожидает ревью специалиста |
| Прошивка ПДА / LoRa / UWB | ⏸ **Отложено** — после согласования логики и механик |

**Следующий шаг (на 17.06):** финальная проверка платы у PCB-специалиста. Параллельно — довести логику и механики в `MASTER_SPECIFICATION.md` / `ИГРОВОЙ_СПРАВОЧНИК.md`.

---

## Документация — сводный канон (июнь 2026)
*Дата: 25 июня 2026*

Единый свод согласованных решений — **`docs/COMMUNICATION_CANON.md`**. Ниже — краткая выжимка; при расхождении приоритет у сводного файла и `MASTER_SPECIFICATION.md`.

### Статус

| Направление | Статус |
|-------------|--------|
| Схема и железо | ✅ Утверждено (`REPORTS` 17.06) |
| KiCad v3.0 | ⏸ Ревью PCB-специалистом |
| Логика и механики | 🔄 До прошивки |
| Прошивка ПДА / LoRa / UWB | ⏸ После согласования логики |

### Каналы связи

| Тема | Канон |
|------|--------|
| **Точные метры, зоны** | **UWB (BU03)**, `AT+SETDEV` |
| **LoRa** | **Только Мастер-Пульт** (+ ретрансляция, опц. якоря) |
| **RSSI** | Только «рядом / слышимость», **не** дистанция |
| **Терминалы** (банк, касса, квест, мастерская) | **EEPROM CH1** — handoff @ `0x00` или кабель + TXN @ `0x80` |
| **Устарело** | LoRa ПДА↔терминал, `EXT_REQ`, RSSI-калибровка 5/10/20 м |

### Слоты TCA

CH1 — универсал + кабель · CH2 — броня · CH4–6 — артефакты · эталон `mux_channels.h`

### Обновлённые файлы

`docs/COMMUNICATION_CANON.md` (новый) · `MASTER_SPECIFICATION.md` · `ROADMAP.md` · `BOARD_SPEC_v3_FOR_REVIEW.md` · `ИГРОВОЙ_СПРАВОЧНИК.md` · `docs/PROGRESSION.txt` §9–§10 (терминал + ПК-приложение)

---

## ROADMAP — каналы связи и внешние терминалы
*Дата: 17 июня 2026 · уточнено 25.06*

Исправлены устаревшие формулировки в **`ROADMAP.md`**:

| Тема | Было (ошибка) | Стало (канон) |
|------|---------------|---------------|
| Банкомат, касса, квесты, мастерская | LoRa-запросы ПДА↔терминал | **EEPROM CH1** — handoff-чип или кабель + TXN @ `0x80` |
| LoRa | «всё дальнее» включая терминалы | **Только Мастер-Пульт** (+ опц. якоря, ретрансляция) |
| Позиционирование | Калибровка RSSI 5/10/20 м | **UWB (BU03)** + `AT+SETDEV`; RSSI — только присутствие |
| Фаза 7 (терминалы) | LoRa-ноды, `EXT_REQ` | Терминалы — **фаза 5.7**, **EEPROM**, без радио ПДА↔терминал |

> Полная спецификация TXN и карта каналов — запись **«EEPROM TXN-протокол»** ниже (22.06.2026).

---

## Согласование документации и эталонов (v6.0)
*Дата: 17 июня 2026*

**Зафиксировано как канон:**

| Тема | Значение |
|------|----------|
| Габариты PCB | LOGIC и POWER: **140 × 78 mm** |
| KiCad | **`hardware/STALKER_PDA_v3.0/`** |
| J_INTER | **1×15** (2.54 mm) |
| Слоты | **TRRS ×4 + J_TRS на CH1**; CH1 универсал + кабель, CH2 броня, CH4–6 артефакты (`mux_channels.h`) |
| BU03 UART | **G1/G2**, `BU03_PINS_PLATE 1` |
| LED G16 | **100 Ω** |
| Зелёная зона | **5 с** без пакетов SAFE_ZONE |
| Дисплей | **ILI9341 320×240** |
| HTML | только **`hardware/schematic.html`** |

**Обновлены:** `MASTER_SPECIFICATION.md` v6.0, `ROADMAP.md`, `BOARD_SPEC_v3_FOR_REVIEW.md`.  
Удалены дубли и битые фрагменты в MASTER; убраны устаревшие ссылки на v2, ST7735, DW1000 как актуальное железо.  
Чип «РАНГ» в EEPROM — **не реализован**. Зарядный модуль — вне scope.

---

> Записи **до 17.06.2026** ниже — исторический журнал; актуальные эталоны — в блоке «Согласование документации» выше и в `MASTER_SPECIFICATION.md` v6.0.

## BOARD_SPEC v3 + KiCad v3.0 — макет для проверки
*Дата: 17 июня 2026*

- Добавлен **`hardware/BOARD_SPEC_v3_FOR_REVIEW.md`** — краткая спека для разработчиков PCB (блоки, обвязка, расстановка из KiCad).
- **Эталон логики:** `hardware/schematic.html` (не KiCad stub-схема).
- **Эталон координат:** `hardware/STALKER_PDA_v3.0/` — LOGIC + POWER; J_INTER 1×15; TRRS ×4 + **J_TRS на CH1** (см. `mux_channels.h`, `docs/COMMUNICATION_CANON.md`).
- **BU03 100 nF:** только bypass на **3.3 V у модуля** (как C1/C3 у LoRa/DFPlayer). Доп. конденсатор **после P-MOS не нужен**.
- Защита слотов (PTC, 220 Ω I2C) — рекомендация до заказа, в KiCad ещё не разведена.

---

## Схема — трассировка 5V / вибро / TFT
*Дата: 17 июня 2026*

- ESP 5Vin огибает TFT сверху; 5V вибро — вниз под ESP, стык с **ESP 5Vin** (`esp5vBendX`), не через MUX/корпус.
- D1 слева от мотора; коллектор Q1 — снизу к M−; 5V от M+ — вправо (не через D1/C).

---

## Схема — блок питания BU03 (P-MOS)
*Дата: 17 июня 2026 (2)*

- LoRa и BU03 выровнены по одному центру, увеличен зазор; антенны разнесены (LoRa слева, BU03 справа).
- G42 обходит корпус BU03 сверху; C2 на вертикали VCC (ответвление влево); подписи AO3401 вынесены вправо от канала.

---

## Схема — блок питания BU03 (P-MOS)
*Дата: 17 июня 2026*

- `hardware/schematic.html`: **BU03 POWER KEY** перенесён **справа и выше** модуля BU03; VCC — одна вертикаль сверху; **C2** на шине над модулем; P-MOS AO3401 перерисован (G/S/D).

---

## Схема — блок питания BU03 (P-MOS)
*Дата: 16 июня 2026*

- `hardware/schematic.html`: блок **BU03 POWER KEY** перерисован — G42 → 220Ω → 2N2222 → AO3401 (S→3V3, D→VCC, R8 10k), без наложений проводов.

---

## Test_PDA_Hardware — диагностика BU03 (0 bytes UART)
*Дата: 13 июня 2026*

**Симптом:** G42 / P-MOS «включает питание», но `AT` → `rx=''` (0 байт), PMOS/UWB FAIL.

**Причина:** **питание ≠ UART.** Светодиод или частичное 3.3V не означает, что STM на BU03 отвечает по Serial1.

**Прошивка `test_firmware/Test_PDA_Hardware/Test_PDA_Hardware.ino`:**
- `#define BU03_PINS_PLATE 1` — плата **G1/G2**; `0` — макет **G17/G18**
- `BU03_BOOT_MS` **3500** ms после G42 HIGH
- Экран/Serial: чеклист (VCC после P-MOS, GND, swap TX/RX, USB-UART тест модуля)

---

## ПДА v2.7 — экран UWB / дистанция (тест BU03)
*Дата: 13 июня 2026*

- `proshivki/Pda/V1/PDA_ESP32.ino` **v2.7**: 4-я страница «UWB / ДАЛЬНОСТЬ» (индекс 3), опрос `AT+DISTANCE` ~750 ms, инициализация BU03 на boot (Serial1 **G1/G2**, **G42** PWR).
- Перепайка не нужна, если `Test_PDA_Hardware` UWB уже PASS на этих пинах.

---

---

## XP / уровни — ребаланс v1.2
*Дата: 17 июня 2026*

**Справочник:** `hardware/PROGRESSION.txt` **v1.2** §2, §6–7; `programmat_pc/game_logic.py`.

**Изменения:**
- **Квесты:** при сдаче `complete_quest()` начисляет **XP = RUB** (`rub_reward` на чипе) + деньги; вехи G01/G02/G03 — **бонус** (XP снижен ~50%).
- **Формула уровней:** четыре линейные зоны вместо квадратичной; суммарно до ур. 100 ≈ **41 625 XP** (было ~587k).
- **Темп:** ур. 10 за ~1.5–2 ч активной игры; ур. 100 за ~6–7 игровых дней при 4–6 ч/день.
- **`game_logic.py`:** поле `rub_reward` на квест-чипе (бокс 1, по умолчанию 300).

**Без изменений:** допуск/воскрешение, combat lock, ранги (`RANK_PREREQ_ACH`), вибро-whitelist.

---

## Версия 6.8 — Прогрессия v4, TCA CH0–CH4, ПДА v2.4
*Дата: 12 июня 2026*

**Справочник прогрессии:** `hardware/PROGRESSION.txt` (уровни 1–100, 6 рангов, 62 достижения, пороги механик).  
**Спецификация:** `MASTER_SPECIFICATION.md` **v5.4** §8.5.

**Слоты TCA9548A (TRRS) — финал:**
| CH | Назначение |
|:---:|---|
| 0 | Броня |
| 1–3 | Артефакты 1–3 |
| 4 | Универсальный — расходник, админка, магазин, квест, банкомат (чип пишет программатор, ПДА читает при вставке) |
| 5–7 | Не используются |

**Ранги (подтверждение мастером):** только **RUB + скидка магазина**, XP за ранг **не** начисляется.

| Ранг | Мин.ур. | RUB | Скидка |
|---|---|:---:|:---:|
| НОВИЧОК | 1 | 0 | 0% |
| БРОДЯГА | 10 | 800 | 3% |
| СТАЛКЕР | 25 | 1500 | 6% |
| ВЕТЕРАН | 40 | 2500 | 10% |
| МАСТЕР | 60 | 4000 | 14% |
| ЛЕГЕНДА | 80 | 6000 | 18% |

**Прошивка `proshivki/Pda/V1/PDA_ESP32.ino` v2.4:**
- `RankTier` с `rubReward` / `discountPct`; `confirmNextRank()` — RUB + скидка на экране, без XP
- `getRankDiscountPct()`, `getLevelTitle()` — титул ур. 100: **ХРОНИКЁР ЗОНЫ** (не путать с рангом ЛЕГЕНДА)
- Пороги: `LVL_HIDDEN_QUEST=20`, `LVL_DETECTOR=25`, `LVL_GLOBAL_MSG=50` + хелперы `canUse*()`
- «ESP-датчик» переименован в UI/доках → **ДЕТЕКТОР АНОМАЛИЙ** (ур. 25+)
- Глобальные сообщения (ур. 50+) — заглушка / TBD (платная услуга)

---

## Версия 6.7 — ПДА v2.1: пины под валидированное железо
*Дата: 12 июня 2026*

**Эталонная прошивка ПДА:** `proshivki/Pda/V1/PDA_ESP32.ino` **v2.1** (не тестовая `Test_PDA_Hardware`).

**Обновлено под MASTER_SPEC §3 и стенд `Test_PDA_Hardware`:**

| Было (v2.0 / прототип) | Стало (v2.1) |
|---|---|
| ST7735 160×128 | **ILI9341** 320×240, UI 160×128 в углу |
| 5 кнопок UP/DN/L/R/OK (G15) | **4 кнопки** OK/ESC/RIGHT/DOWN G4–G7 |
| LED G48/G1 | **G16/G15** |
| Вибро G2 (конфликт BU03) | **G21** (5V BUS) |
| DFPlayer begin без probe | **initDfPlayer()** timeout + readState |
| Нет SPI sharing | **TFT_CS/LORA_CS** idle HIGH, MISO G40 |
| Нет тумблеров | **SND G47, VIB G48** — звук/вибро по тумблеру |
| Нет BU03_PWR | **G42**, `HAS_BU03_PWR=1`, Serial1 G1/G2 |

**Сохранено без изменений:** UI (3 страницы), меню, ESP-NOW, NVS, CONFIG:FUNC/PRESET, игровая логика HP/RAD/ЗЗ.

**Советы по сборке (сессия):**
- DFPlayer + вибро — **5V BUS (TPS63020)**, не от USB ESP; 5VIN — вход; USB ~500 mA недостаточно
- Тест-сборка: `test_firmware/Test_PDA_Hardware` — пошаговая проверка на экране
- Звук: без 5V на DFPlayer — probe fail, прошивка продолжает работу
- LoRa: общая SPI с TFT; **не пульсировать RST G12** для LoRa в runtime
- Старые пины `proshivki/Pda/V1` v2.0 (ST7735, 5 кнопок, G1/G2 вибро) — **не использовать**

**Сборка:** Board ESP32S3 Dev Module, PSRAM OPI, Upload **115200** при сбоях esptool. Библиотеки: Adafruit GFX, **ILI9341**, DFPlayer Mini, U8g2_for_Adafruit_GFX.

---

## Версия 6.6 — Синхронизация документов (аудит)
*Дата: 11 июня 2026*

**Исправлены расхождения между MASTER_SPEC, ROADMAP, komponenty.txt:**

| Было (ошибка) | Стало (эталон) |
|---|---|
| LED зелёный **110 Ω** (ROADMAP) | **100 Ω** (G16) |
| `proshivki/PDA_ESP32.ino` | `proshivki/Pda/V1/PDA_ESP32.ino` |
| `Field_ESP32.ino` без пути | `proshivki/Anomaly_GZ/Field_ESP32.ino` |
| REPORTS v6.3 в komponenty.txt | **v6.5** |
| Дата MASTER_SPEC 10.06 | **11.06.2026** |

**Намеренно устаревшие (история / не трогать):**
- `REPORTS.md` v5.4 и ниже — архив решений (DW1000, TP4056, 110 Ω)
- `плата/Плата вариант 1.txt` — JSON-макет v1 (TP4056, MT3608, DW1000)
- `Chip_Programmer_ESP32.ino` — ESP32-C3 (отдельное устройство программатора)

**Эталон для сборки:** `schematic.html` + `komponenty.txt` + `MASTER_SPECIFICATION.md` §3 + `POTREBLENIE.pdf`.

---

## Версия 6.5 — Аудит обвязки, BOM, питание, полевые устройства
*Дата: 11 июня 2026*

**1. Пассивка и схема ПДА:**
- Аудит `schematic.html` + `komponenty.txt` — обвязка **достаточна** для макета
- Добавлен **R8 10 kΩ** на затвор P-MOS (BU03_PWR G42); R7 = RST G12

**2. BOM и компоновка:**
- Тумблеры: **SND (G47) + VIB (G48)** + BL 3-поз.; RAD-тумблер убран
- EEPROM ×5 — **внешние TRRS-картриджи**; зарядка **IP2312**; DC-DC **2× TPS63020**
- **Вибромотор — только 5V BUS** (не 3.3V)

**3. Полевые устройства:**
- **ESP32-S3 + BU03 + LoRa**; пины как на ПДА; BOM пассивки в `komponenty.txt`
- Питание поля: IP2312 + 1× TPS63020 3.3V

**4. Потребление:** `hardware/POTREBLENIE.pdf` + `generate_power_pdf.py`

**5. Документы синхронизированы:** `MASTER_SPECIFICATION.md` v5.2, `ROADMAP.md`, `komponenty.txt`

---

## Версия 6.5 — Стенд TCA9548A + EEPROM: PASS на WROOM32 и S3
*Дата: 10 июня 2026*

### Аппаратные решения (подтверждены стендом)

| Решение | Детали |
|---------|--------|
| Одна I2C ESP → TCA | Отдельные GPIO на каждый EEPROM **не нужны** |
| Выбор чипа | `tcaSelect(ch)` → `Wire` к **0x50** |
| TCA A0–A2 | **GND** → адрес **0x70** |
| TCA RST | **3.3 V** |
| I2C S3 (ПДА) | SDA **G9**, SCL **G8** |
| I2C WROOM32 (стенд) | SDA **G21**, SCL **G22** |
| EEPROM | CH0/CH1 @ **0x50**; WP, A0–A2 → GND |
| Подтяжки | 4.7 кОм (допустимо **4.4 кОм**), одна пара на главной шине |
| Обвязка I2C | Транзисторы/конденсаторы на каждый EEPROM **не нужны** |

### Прошивка

- `test_firmware/EEPROM_TCA_Test/EEPROM_TCA_Test.ino` (v1.1) — WROOM32/S3 через `#ifdef`, команды `TEST`/`SCAN`/`HELP`.
- `test_firmware/EEPROM_TCA_Test/README_EEPROM_TCA_Test.md` — распиновка, безопасность (горячий GND = КЗ).

### Результаты

| Плата | I2C | TCA | CH0 | CH1 | Итог |
|-------|-----|:---:|:---:|:---:|:----:|
| ESP32-WROOM32 | G21/G22 | OK | R/W | R/W | **PASS** |
| ESP32-S3 | G8/G9 | OK | R/W | R/W | **PASS** |

Тест @ `0x0100`; перезапись EEPROM подтверждена.

### Документы

- `MASTER_SPECIFICATION.md` v5.1 — §3 TCA/EEPROM, §14 тест, диагностика.
- `ROADMAP.md` — Фаза 3: стенд EEPROM отмечен выполненным.

### Замечания

- Горячий GND на S3 при первом включении — КЗ в проводке; после исправления — PASS.
- Прошивка S3: при `chip stopped responding` — Upload Speed **115200**.

---

## Версия 6.4 — Схема BU03, интеграционный тест, стенд «всё вместе»
*Дата: 10 июня 2026*

**1. Интерактивная схема** (`hardware/schematic.html`):
- UWB **DW1000 SPI** → **BU03-Kit UART AT**
- Пины: **G1** RX ← PA2, **G2** TX → RX, **G42** `BU03_PWR` (2N2222 + P-MOS)
- **G12** — RST только TFT + LoRa; BU03 со своим STM32

**2. BOM** (`hardware/komponenty.txt`): BU03-Kit, 2× 2N2222, P-MOS.

**3. Стенд ESP32-S3 (по отдельности OK):** TFT ILI9341, LoRa Ra-01, BU03 (`AT+DISTANCE`), **TCA9548A + 2× EEPROM** (см. v6.5).

**4. Прошивка** `test_firmware/Test_PDA_Hardware/` — guided on-screen (TFT+LoRa+BU03); флаг `HAS_BU03_PWR`.

**5. Совмещение периферии (не GPIO, а поведение):**
- SPI shared TFT+LoRa — один CS; не рисовать экран во время LoRa SPI
- G12 shared RST — reset одного = reset обоих
- UART: BU03 `Serial1` G1/G2, DFPlayer `Serial2` G17/G18
- Питание: не LoRa TX + UWB ranging одновременно
- RF: 433 МГц / 2.4 / 6.5 ГГц — разные; антенны не наслаивать

**6. KiCad** `hardware/STALKER_PDA_v2/` — **ещё DW1000**; HTML-схема опережает PCB.

**7. Документы:** `MASTER_SPECIFICATION.md` (§3, §14), `ROADMAP.md` (Фазы 3–4).

---

## Версия 6.3 — LoRa: breadboard-тест и протокол
*Дата: 9 июня 2026*

**1. Решения по архитектуре радио:**
- **LoRa** — дальняя связь (команды, Выброс, внешние устройства, ретрансляция на полигоне ~3×3 км).
- **UWB (BU03)** — точные метры до аномалии / вход в ЗЗ.
- **ESP-NOW** — короткая дистанция, игровые пакеты (DAMAGE, ACK).
- LoRa **не использует MAC** — передача на частоту 433 МГц; адресация в payload: `dest_id`, `src_id`, `type`, CRC16.
- **Грубая зона** на большом полигоне — по «**какие якоря услышали**» приmа, **не** RSSI-триангуляция.
- **Ретрансляция (план):** ESP получил пакет → при `TTL > 0` пересылает дальше по цепочке.

**2. Breadboard-стенд (2× ESP32 DevKit + 2× Ra-01):**
- Скетчи: `test_firmware/LoRa_Test_Sender/`, `test_firmware/LoRa_Test_Receiver/` (отдельные папки).
- Временные пины макета: SCK=18, MISO=19, MOSI=23, CS=5, RST=14, DIO0=26.
- Целевые пины S3 без изменений: CS=39, MISO=40, DIO0=41, RST=12, SCK=13, MOSI=14.
- Библиотека **LoRa** (Sandeep Mistry) — установка **вручную** с GitHub (Library Manager → 403 Forbidden).

**3. Результат теста:**
- Код и распайка **верны** — рабочий Ra-01 инициализируется (`LoRa OK`).
- **Swap-тест:** один из двух Ra-01 **неисправен** (`LoRa init FAILED`, SPI не отвечает) — требуется **замена модуля**.
- Полный ping-pong — после прихода второго живого Ra-01.

**4. Документы обновлены:** `MASTER_SPECIFICATION.md` (§3 LoRa, §14 тест), `ROADMAP.md` (Фаза 4).

---

## Версия 6.3 — Итог сессии 9 июня 2026 (BU03 + документация)
*Дата: 9 июня 2026*

### Решения по UWB (BU03-Kit)

| Тема | Принято |
|------|---------|
| Модуль | **BU03-Kit** вместо **DW1000 SPI** на ПДА, аномалии, убежище |
| Стек | STM считает TWR; ESP — **UART AT** + ESP-NOW |
| Канал | **Один** `CH:1` на весь полигон |
| Аномалия | Anchor на точке; ПДА = tag; **динамические слоты** 0–10 |
| Убежище | Tag с **фикс. `uwb_zone_id`**; ПДА у входа = **anchor** → `ENTRY_OK` |
| Удар | Порог по **уже измеренной** дистанции |
| Питание | **3.3 V**; OFF через **G42 + 2N2222 + P-MOS** (ПДА + полевые точки) |
| Точки | **Одна аномалия в точке**; две рядом — отложено |
| DW1000 | **Не совместим** с BU03 в одной сети; не использовать |
| Схема | `STALKER_PDA_v2` — **переделка**; макет G17/G18, плата **G1/G2** |

### Прошивки и тесты

- `test_firmware/BU03_Distance_AT.ino` — дистанция на TFT (стенд OK)
- `test_firmware/BU03_ID_Test.ino` — смена ID через ESP
- Удалён отдельный `BU03_UWB_ARCHITECTURE.md` — всё в **трёх основных документах**
- Удалён ранее бинарный `BU03_Distance_Display.ino` (заменён AT-подходом)

### LoRa (та же сессия)

- Breadboard-тест: `LoRa_Test_Sender/`, `LoRa_Test_Receiver/`
- LoRa = дальняя связь; UWB = метры у точки; ESP-NOW = игра
- Библиотека LoRa (Mistry) — ZIP с GitHub

### Документы обновлены

- `MASTER_SPECIFICATION.md` — §3 BU03/LoRa, §7 UWB-логика, §14 тесты
- `ROADMAP.md` — Фаза 4, тесты, ЗЗ, отложенные задачи
- `REPORTS.md` — этот журнал

---

## Версия 6.2 — UWB в трёх основных документах + убежище
*Дата: 9 июня 2026*

**1.** Весь материал по BU03 перенесён в **`MASTER_SPECIFICATION.md`** (§3 железо, §7 UWB-логика) и **`ROADMAP.md`** (Фаза 4). Отдельный файл `BU03_UWB_ARCHITECTURE.md` **удалён**.

**2. Убежище (зелёная зона), согласовано:**
- BU03 на убежище = **tag** с **фиксированным UWB ID зоны** (`uwb_zone_id` в NVS)
- ПДА у входа переключается в **anchor**, меряет дистанцию, шлёт **`ENTRY_OK`**
- Динамические слоты **не нужны** для ЗЗ

**3.** Аномалия без изменений: anchor на точке, tag на ПДА, динамические слоты.

---

## Версия 6.1 — UWB: переход на BU03-Kit
*Дата: 9 июня 2026*

**1. Решение:** дальномер — **BU03-Kit** (UART AT), не DW1000 по SPI.

**2. Согласовано:** один канал `CH:1`; питание 3.3 V + ключ на ПДА и точках поля; одна аномалия в точке; схема PCB — переделка после теста платы.

**3. Тесты:** `BU03_Distance_AT.ino`, `BU03_ID_Test.ino`.

---

## Версия 6.0 — Рефакторинг структуры проекта
*Дата: 8 июня 2026*

**1. Документация объединена в три файла:**
- `HARDWARE_PINOUT_S3.md`, `PROM_PINOUT.md`, `pcb_design_guidelines.md` → **MASTER_SPECIFICATION.md**
- BOM: `hardware/komponenty.txt` (из `Список компонентов.ini`)

**2. Переименование папок (транслит):**
| Было | Стало | Статус |
|------|-------|--------|
| `firmware/` | `proshivki/` | ✅ |
| `pda_simulator/` | `programmat_pc/` | ✅ |
| `mobile_programmer/` | `mobilny_programmat/` | ✅ |
| `test_firmware/` | `testy/` | ⏳ вручную (файл открыт в IDE) |
| `hardware/` | `plata/` | ⏳ вручную |

**3. Прошивки:** удалены черновики `_Ranger_S3`, `_Anchor_S3`, `Combined_Anchor_S3`, `screen_mockup.html`.  
Остались: `Field_ESP32`, `PDA_ESP32`, `Chip_Programmer_ESP32`.

**4. Тесты:** в `test_firmware/` оставлен только `Test_PDA_Hardware.ino` (остальные UWB/LoRa-скетчи удалены).

**5. Плата:** актуальный проект — `hardware/STALKER_PDA_v2/`. К удалению (мусор v1):
- `hardware/STALKER_PDA/`, `hardware/STALKER_PDA_*` в корне `hardware/`
- `hardware/.history/`, `STALKER_PDA_v2/.history/`
- папка `плата/` (дубликат gerber)
- `tools/` (скрипты для v1)

**6. Добавлен корневой `.gitignore`** (build, .history, KiCad temp).

---

## Версия 5.4 — Схема железа ESP32-S3 + UWB DW1000
*Дата: 23 апреля 2026*

**Суть:** Полная ревизия аппаратной части. Схема `wokwi/schematic.html` стала единственным источником истины по железу.

**1. Плата:** ESP32-WROOM-32 → **ESP32-S3-N16R8** (16MB / 8MB PSRAM / LX7 / 240MHz)

**2. Новые компоненты в схеме:**
- **LoRa SX1278 Ra-01(+ANT) 433МГц** — SPI shared с TFT (CS=G39, RST=G40, DIO0=G41)
- **UWB DW1000 6.5GHz** — SPI shared, CS=G1, IRQ=G2, RST=G42 (ранее свободные пины)
- **TCA9548A** I2C мультиплексор + **5× TRRS**; EEPROM 24LC256 — внешние картриджи (CH0-4)
- Вибромотор LRA через NPN 2N2222 (G21)
- Тумблеры: SND(G47), VIB(G48), BL_ON(G37), BL_AUTO(G38); 2× ON/OFF + 1× ON/OFF/ON
- LED красный G15 (220Ω), LED зелёный G16 (110Ω)

**3. Изменение питания:**
- Вибромотор переключён с 3V3 → **5V BUS** (снижение нагрузки на встроенный LDO)
- DFPlayer уже был на 5V — подтверждено
- LoRa/UWB/TFT/TCA9548A остались на 3V3 (нельзя на 5V — чипы 3.3V)

**4. Анализ тока 3V3 (LDO AP2112K 600mA):**
- Пик: LoRa TX (120mA) + UWB ranging (105mA) + TFT (80mA) + misc = ~345mA + ESP ядро ~200mA ≈ **545mA**
- Firmware constraint: **не запускать LoRa TX + UWB ranging одновременно**
- При нестабильности: добавить AMS1117-3.3 (1A) внешний LDO

**5. Документы обновлены:**
- `MASTER_SPECIFICATION.md` — ЧАСТЬ 14 полностью переписана под ESP32-S3
- `ROADMAP.md` — Фаза 3 обновлена (правильные GPIO), Фаза 4 (LoRa) помечена как "железо готово", добавлена Фаза 4.5 (UWB), удалена Фаза 99
- `HARDWARE_PINOUT_S3.md` — создан новый файл с полной таблицей пинов

**6. Схема исправлена:**
- Устранены JS ошибки: `btnGroupY` не объявлена, `DF_DET_Y` не объявлена, `res:` без значения
- Восстановлены удалённые рамки (батарея АКБ, секция MP3)
- Восстановлен динамик в детальном виде MP3-TF-16P

---

## Версия 5.3 (Расходники + Админка + ПДА КОНФИГ)

*Дата: 22 апреля 2026*

**Суть:** Финализация всех типов чипов: расходники +3 подтипа, админка +4 подтипа, новое устройство ПДА КОНФИГ.

**1. РАСХОДНИКИ (type=0) — 3 новых подтипа:**
- **sub=3 СТИМУЛЯТОР:** Временный буст 8 типов защит на N секунд. `prot[i] = min(100, base + p_i)` пока таймер > 0.
- **sub=4 ВОССТАНОВЛЕНИЕ:** Ремонт/зарядка брони и артефактов. Режим ЕД/%, кэп maxUses.
- **sub=5 УЛУЧШЕНИЕ:** Постоянный апгрейд — доп. прочность (макс 250) + % ко ВСЕМ параметрам. Абсолютный кэп прочности = 250.

**2. АДМИНКА (type=3) — 4 новых подтипа:**
- **sub=2 УРОВЕНЬ/ОПЫТ:** LVL (+/-) + XP (+/-).
- **sub=3 ИММУНИТЕТ:** Длительность в минутах. Весь входящий урон = 0.
- **sub=4 СБРОС:** Атомарный откат: HP=maxHP, RAD=0, эффекты=0, деньги=стартовые.
- **sub=5 НЕЙТРАЛИЗАЦИЯ:** Принудительный STATE_DEAD. Красный текст в UI.
- ПРЕСЕТ (старый sub=2) удалён → заменён ПДА КОНФИГ.

**3. ПДА КОНФИГ (dev=3) — НОВОЕ УСТРОЙСТВО:**
- Прямое USB-подключение ПДА к ноутбуку (не через чипы)
- Режим ФУНКЦИИ: 8 чекбоксов вкл/выкл модулей (HP, RAD, деньги, броня, арты, аномалии, уровни, чипы)
- Режим ПРЕСЕТ ДАННЫХ: maxHP, startHP, maxRAD, деньги, уровень, XP + 8 базовых защит %
- Serial: `CONFIG:FUNC:flags=<byte>` / `CONFIG:PRESET:maxhp=...,xp=...,r0..r7=...`
- Хранение в NVS (Preferences) — сохраняется при poweroff

**4. Документация:** MASTER_SPEC, ROADMAP, REPORTS обновлены.

**5. Баги:** `(0,0).__class__()` → tuple fix; наложение строк в ПРЕСЕТ ДАННЫХ.

---

## Версия 5.2 (Программатор чипов — полная реализация UI)
*Дата: 21 апреля 2026 (вечерняя сессия)*

**Суть:** Полностью дописан интерфейс программатора чипов (`programmer.py`). Все типы чипов реализованы и проверены на соответствие `Chip_Programmer_ESP32.ino`.

**1. Система HP/мин + интервал тика (везде)**
- В убежище и артефактах поле ввода — **HP/МИН**, интервал — **ЧЧ:ММ:СС**
- Формула: `hp_per_tick = round(hp_per_min x interval_sec / 60)` — применяется при прошивке

**2. Убежище:** метка `ОЧИСТКА RAD (ЗА ТИК)` -> `ОЧИСТКА RAD:` / `RAD/мин`

**3. Артефакты (type=2):**
- Строка РЕГЕН HP/МИН + строка ИНТЕРВАЛ РЕГЕНА ЧЧ:ММ:СС
- Таблица: 7 HP-типов x (защита % | урон HP | интервал ЧЧ:ММ:СС на каждый тип)
- Секция РАДИАЦИЯ отдельно (после разделителя): защита % | +RAD/мин | интервал ЧЧ:ММ:СС
- build_config_str: p0=HP/тик, p1=интервал(сек), p2..p9=защита%, p10..p15=урон HP (6 типов)

**4. ПРОЧНОСТЬ** перенесена в самый низ для всех типов чипов (над COM-портом)

**5. Проверка соответствия экосистеме:**
- programmer.py <-> Chip_Programmer_ESP32.ino: полное соответствие
- EEPROM: 0x00=type, 0x01=sub, 0x02=uses, 0x04..0x23=params[16 x int16], CRC16 в 0x24..0x25
- ROADMAP.md обновлён — секция ЧИП переведена в "Уже реализовано"

**Known Gap (не блокирует текущую фазу):**
- Урон по ГРАВИТ. и +RAD/тик не влезают в p10..p15 (6 слотов) — задача Фазы 3
- Per-type интервалы урона артефакта — UI-only, в EEPROM не записываются — задача Фазы 3

---

## Версия 5.1 (Разделение РАДИАЦИИ и HP-урона)
*Дата: 21 апреля 2026*

**Суть изменения:** Радиация полностью отделена от системы HP-урона. Ранее она передавалась как бит 5 в `dmg_mask` вместе с ударом HP. Теперь — это независимый пакет с независимым таймером.

**1. Протокол (`protocol.py`):**
- `DmgType.RADIATION` (бит 5 dmg_mask) — **упразднён как HP-тип**, помечен как зарезервированный
- Активные HP-биты: `0,1,2,3,4,6,7` (7 типов, бит 5 пропущен)
- Добавлена константа `DMGTYPE_HP_BITS = [0,1,2,3,4,6,7]` — список активных битовых позиций HP-маски для правильного маппинга чекбоксов (без него чекбокс №5 «ПСИ» неправильно ложился бы в бит 5, а не 6)
- Добавлена функция `create_radiation_packet(rad_amount)` → `Msg.RADIATION`
- `Msg.DAMAGE` — теперь строго только HP, `val2=0` всегда

**2. Прошивка аномалии (`Test_Anomaly_v4.ino`):**
- Два независимых таймера: `lastSendMs` (HP) и `lastRadMs` (RAD)
- `sendPacket()`: `val2=0`, бит 5 dmg_mask зарезервирован, не используется
- `sendRadPacket()`: отдельный `MSG_RADIATION` с `val1=cfg.rad_amount`
- Конфиг: `rad_amount` (RAD за удар), `rad_freq` (интервал RAD-таймера в мс) — независимы от `dmg_amount` и `frequency`

**3. Программатор аномалий (`programmer.py`):**
- Чекбоксы HP-типов: **7 штук** (ВЗРЫВ, КРОВЬ, ТЕРМО, ЭЛЕКТРО, ХИМИЯ, ПСИ, ГРАВИТ.) — РАДИАЦИЯ убрана
- Добавлена строка РАДИАЦИЯ (ниже чекбоксов): галочка вкл/выкл + поле RAD/уд | таймер ЧЧ:ММ:СС
- Зона клика галочки = 105px (не перекрывает поле ввода)
- `build_config_str` → `rad_dmg=X, rad_frq=Y` только если галочка включена, иначе `0`

**4. Игровая логика (`game_logic.py`):**
- `apply_damage_multi()` — убран параметр `rad_amount`, бит 5 маски игнорируется
- Добавлен `self.last_radiation_time` — отдельный кулдаун для `Msg.RADIATION`
- `innate_resistance` и `damage_taken_accumulator` — убран ключ `5` (нет HP-сопротивления к радиации)

**5. Эмулятор (`full_emulator.py`):**
- DAMAGE-обработчик: убран `rad_amount=pkt.val2`, комментарий обновлён

**6. Документация (`MASTER_SPECIFICATION.md`):**
- Таблица DmgType: бит 5 → ~~RADIATION~~ **ЗАРЕЗЕРВИРОВАН**
- Таблица DAMAGE: добавлено предупреждение об отдельном пакете RAD
- Примеры кода: добавлен `create_radiation_packet()`

---


*Дата: 20 апреля 2026*

**1. Интеграция дисплея ST7735 (Шаг 1):**
- Осуществлен первый успешный порт UI из Python-симулятора на микроконтроллер ESP32-WROOM-32 (развертка 160х128).
- **Победа над кириллицей:** Для корректной поддержки UTF-8 русского языка поверх драйвера `Adafruit_ST7735` была интегрирована библиотека-обертка `U8g2_for_Adafruit_GFX` (использован шрифт `u8g2_font_5x8_t_cyrillic`). Это позволило избежать костылей с перекодированием или переводом интерфейса на английский.
- Полностью отрисованы 3 экрана (Показатели, Инвентарь, Сопротивления) и строка системных логов.

**2. Интеграция кнопок ввода (Шаг 2):**
- Подключены и прошиты 3 тактовые кнопки на пины 32 (ВНИЗ), 33 (ВПРАВО) и **27** (ОК / Перевешена с 25 пина для устранения аппаратных наводок).
- Использована внутренняя подтяжка `INPUT_PULLUP` (минимум обвеса на плате).
- Написан **сверхнадежный программный антидребезг (debounce 50мс)**, реагирующий строго на Edge Detection (смену состояния), что устранило программное залипание-автоогонь при удержании кнопок.
- Настроена полная навигация по экранам и курсору инвентаря.

**2.5 Интеграция ESP-NOW и АСК (Шаг 4):**
- Аномалия успешно прошита тестовым кодом (спам пакетами Урона Взрыва 10 HP каждые 5 сек).
- ПДА переведен на **21-й пин** для `TFT_DC`, чтобы исключить конфликт с `WiFi.mode()`, который зависал плату при инициализации радиомодуля.
- Внедрен механизм получения урона и мгновенной отправки **ACK-пакета** (с вложенным значением текущего HP).
- Двусторонняя связь ESP-NOW верифицирована (Аномалия фиксирует попадания с указанием актуального здоровья жертвы в Serial Monitor).

**2.6 Интеграция Аудио, LED-индикации и Вибрации (Фаза 3):**
- Интегрирован модуль **DFPlayer Mini (MP3-TF-16P)** для звукового сопровождения. Питание строго от `5V (VIN)`, управление через `HardwareSerial 1` (пины TX→17, RX→18).
- Подключены **LED-индикаторы** состояний через токоограничивающие резисторы (200 Ом):
  - **Красный LED (G1)** — кратковременная вспышка (1 сек) при получении любого урона.
  - **Зеленый LED (G48)** — ровное горение при нахождении в «Безопасной Зоне» (`MSG_SAFE_ZONE = 7`).
- Внедрен **Вибромотор (Haptic Feedback)**:
  - Управление моторчиком через **NPN-транзистор 2N2222**, т.к. пин ESP32 не тянет 100мА.
  - База транзистора подключена к **GPIO 2** через резистор 200 Ом.
  - Отдача (vibration) срабатывает на 250 мс одновременно с получением урона.

**3. Рефакторинг Дорожной карты:**
- В `ROADMAP.md` прописан атомарный пошаговый план включения железа (от Шага 1 «Интеграция UI» до Шага 6 «I2C-периферия» и чипы EEPROM). Это позволит локализовывать баги на уровне единичных аппаратных узлов.

---

## Версия 4.2 (Рефакторинг протокола и командная система)
*Дата: 19 апреля 2026*

**1. Реструктуризация `protocol.py` (v4.1 → v4.2):**
- Убрана каша из `SubType` (смешивал урон, команды, выброс) — вместо него 3 чётких enum:
  - `DmgType(IntEnum)` — 8 типов урона (EXPLOSION, BLEED, THERMAL, ELECTRIC, CHEMICAL, RADIATION, PSY, GRAVITY)
  - `CmdSub(IntEnum)` — 7 команд (KILL, REVIVE, WIPE_DATA, ADD_MONEY, SET_PROJECT, ADD_XP, GRANT_RESISTANCE)
  - `ChipType(IntEnum)` — 7 типов чипов (MEDKIT, ANTIRAD, ARMOR, ARTIFACT, CONSUMABLE, RESISTANCE, COMMAND)
- `EmitterClass` → `Emitter`, `MsgType` → `Msg`
- `Msg.MONEY` удалён (ID=4 резерв), деньги только через `Msg.COMMAND + CmdSub.ADD_MONEY`
- `RADIO_TRACK` → `RADIO`

**2. Единая командная система для админских чипов:**
- Все админские действия (воскрешение, деньги, XP, проект, сопротивление) — через `ChipType.COMMAND + CmdSub.*`
- Убраны дублирующие TYPE_XP, TYPE_PROJECT
- Чип воскрешения работает даже на мёртвом игроке

**3. Проекты и названия урона централизованы:**
- `PROJECT_THEMES` из `game_logic.py` перенесён в `protocol.py` как `PROJECTS` + `DMG_NAMES`
- `game_logic.py` импортирует всё из `protocol.py` (единый источник)

**4. Обновление документации:**
- `MASTER_SPECIFICATION.md` обновлён до v4.1 — все таблицы, примеры кода, чеклист для AI-агентов

---

## Версия 4.1 (Мультипроектность и Радио)
*Дата: 18 апреля 2026*

**1. Интеграция мультипроектности (Game Themes):**
- **Протокол:** Значения урона стали абстрактными (`DmgType.EXPLOSION` ... `DmgType.GRAVITY`). Протокол универсален.
- **Логика:** В `protocol.py` добавлен словарь `PROJECTS` с настройками под проекты: S.T.A.L.K.E.R., Fallout, Metro, Tarkov.
- **Переключение:** Смена проекта через `ChipType.COMMAND + CmdSub.SET_PROJECT`.
- **Интерфейс:** UI забирает названия уронов из `PROJECTS`.

**2. Обновление для радиопередач и аудио:**
- В `protocol.py` внедрен пакет `Msg.RADIO = 11`, отвечающий за старт проигрывания треков по радиосигналу от Лоры.
- В аппаратную смету проекта добавлен `DFPlayer Mini` (MP3).

**3. Синхронизация документации (MASTER_SPECIFICATION):**
- Описана логика "Зомби" режима после смерти от 100% радиации/Пси.
- Зафиксировано правило доступности слотов Артефактов.
- Обновлена таблица типов урона с маппингом под все 4 проекта.

---

## Версия 4.0 (Основа и Стабилизация)
*Историческая справка*
- Заложен Python симулятор (Pygame).
- Фиксирован размер UDP пакета (8 байт).
- Механики Выброса, Расходников, Брони.
- Интерфейс на 3 окна: Главный экран, Инвентарь, Характеристики.
